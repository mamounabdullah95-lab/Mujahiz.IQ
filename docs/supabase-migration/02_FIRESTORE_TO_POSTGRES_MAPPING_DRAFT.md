# Firestore-to-PostgreSQL mapping draft

## Status and conventions

- **[Future plan]** This is a logical first pass, not executable SQL. No schema, migration, policy, trigger, function, or table was created.
- **[Verified current fact]** Current shapes come from `src/types/domain.ts`, `src/types/workspace.ts`, `src/services/`, `functions/src/`, `firestore.rbac.rules`, and the live collection inventory in `00_CURRENT_STATE_AND_INVENTORY.md`.
- **[Future plan]** Default identifiers are internal UUIDs unless a deterministic text key is operationally safer. Every migrated entity retains a unique `firebase_legacy_id` or equivalent source-specific field.
- **[Future plan]** Firebase UID is stored as bounded text, not coerced to UUID. Identity mapping must support a later Supabase Auth identity without losing Firebase UID.
- **[Future plan]** `created_at`/`updated_at` use timezone-aware timestamps. Firestore server timestamps require an explicit conversion report for null, pending, string, Date, and Timestamp variants.
- **[Future plan]** Flexible historical payloads may be retained in restricted `jsonb` snapshots for audit, but searchable relationships and authorization fields must be normalized.
- **[Future plan]** “Trusted-only” means browser roles receive no direct write grant; changes occur through reviewed SQL/RPC functions, Edge Functions, or an application service inside a database transaction.

## Identity and organizations

| Proposed table / purpose | Primary key and Firebase legacy field | Main foreign keys | Important unique and check constraints | Soft delete and RLS sensitivity | Trusted-server-only fields | Expected transformation | Open questions |
|---|---|---|---|---|---|---|---|
| `user_profiles` — provider-neutral application person/profile from `users` | UUID `id`; unique nullable `legacy_firestore_id` preserves the source document ID | self-referencing human actors only; provider links and domain tables reference this row | Unique non-null legacy ID; allowed profile status/account context; no email identity relationship | Suspend/deactivate, not cascade-delete; **critical RLS** self/scoped staff | status/context, verification convenience mirror, security eligibility, lifecycle actors | Normalize profile/support fields; retain legacy role/account values as restricted evidence; move provider UID, privilege, ownership, and memberships out | Approved document 33 confirms this UUID as the stable provider-neutral domain principal; ID-001 is Resolved. |
| `identity_provider_links` — exact provider-subject mapping and trusted state mirror | UUID `id`; Firebase UID is bounded `provider_subject` with `provider_code = 'firebase'` | `user_profile_id`; optional migration batch; trusted lifecycle actors | Unique active `(provider_code, provider_subject)`; one active primary link per user/provider; no email matching | Retain unlink/disable/correction history; **critical identity data**, internal/no direct browser access | provider subject, observation state/version/evidence, verification/disablement/unlink actors/times | Create a Firebase link only after exact UID/profile reconciliation; quarantine every collision; future providers add links without rewriting domain FKs | Document 33 prohibits provider-link IDs as domain FKs and silent same-subject relinking across conflicting history. |
| `platform_role_assignments` — temporal platform Owner/Admin authority | UUID `id`; legacy role is evidence, not the key | subject and human actors reference `user_profiles`; later `access_grants` may reference the assignment | One effective active Owner/Admin role per user absent reviewed exception; valid interval/lifecycle; no forward FK to access grants | Append/revoke, never normal-delete; **privileged security data**, fully revoked by default | role, lifecycle, policy version, assignment/revocation provenance | Map only reconciled `owner`/`admin`; never infer from context, email, provider link, ownership, or membership | Required before relational Claim decision runtime but insufficient without current Firebase/profile/link/access/security checks; the empty, fully revoked `public.platform_role_assignments` foundation is selected as the next identity/access structural SQL candidate, with exact SQL still separate. |
| `organizations` — legal/business entity independent of a login | UUID `id`; optional `firebase_legacy_id` when derived from Supplier | none or parent organization later | Unique reviewed normalized legal identity/fingerprint; type/status checks | Soft archive appropriate; RLS depends on public/private columns | canonical name/fingerprint, verification state | Create organizations for Supplier companies; do not auto-merge free-text `users.organization` without review | One table for buyer employers and Supplier companies, or separate entity types? |
| `buyer_profiles` — buyer-specific profile/eligibility | UUID `id`; no separate current document ID | `user_profile_id`, optional `organization_id` | Unique `user_profile_id`; buyer account-context check | Soft deactivate; **high RLS** self/admin | eligibility/access decisions | Split buyer fields from current user document; preserve organization text until matched | Which buyer-company verification fields are required before migration? |
| `supplier_profiles` — canonical listed Supplier | UUID `id`; unique `firebase_legacy_id` from `suppliers/{id}` | `organization_id`, created/approved user IDs | Unique legacy ID; status/verification checks; reviewed canonical fingerprints; non-negative ratings | Soft archive strongly preferred; **high RLS** with public/private projection | `can_receive_rfqs`, approval, verification, normalized identity, quality controls | Split nested branches/contact/taxonomy; preserve original and display names; retain source/audit fields | Which fields are public, buyer-only, owner-only, and admin-only? |
| `supplier_ownerships` — temporal verified primary platform controller | UUID `id`; legacy relation comes from exact agreement between `users.supplierProfileId` and `suppliers.accountOwnerId` | Supplier profile and owner user profile; source-specific claim/submission/event FKs only after those targets exist | One active primary controller per Supplier; no owner-only uniqueness, so one user may own multiple Suppliers; non-overlapping valid intervals | Never hard-delete history; **critical RLS** | activation/closure actor, reason, source class, transfer successor | Reconcile two-sided links; quarantine every mismatch; create no organization or membership by inference | Document 32 is Owner-approved; SUP-001 is Resolved and the empty revoked table is selected as the proposed next local SQL slice. |

Successor note: merged PR #85 implements only the empty revoked `public.supplier_ownerships` foundation. Owner-approved document 34 fixes exactly `owner|admin`, excludes `reviewer` as a platform role, permits many holders per role but only one effective active role per user, defines `active|revoked|expired|superseded` history and a future protected bootstrap of at least two usable Owners, and selects the empty fully revoked `public.platform_role_assignments` table as the next local SQL slice. Exact SQL remains separate.

## Supplier claims, directory, catalog, and import

| Proposed table / purpose | Primary key and Firebase legacy field | Main foreign keys | Important unique and check constraints | Soft delete and RLS sensitivity | Trusted-server-only fields | Expected transformation | Open questions |
|---|---|---|---|---|---|---|---|
| `supplier_ownership_claims` — immutable request/evidence snapshot plus controlled review lifecycle | UUID `id`; unique `firebase_legacy_id` from `supplierOwnershipClaims` when later migrated | claimant user, Supplier profile, reviewer, prior/superseding Claim; decision event only after reliability foundation | `submitted`, `under_review`, `approved`, `rejected`, `withdrawn`, `expired`, or `superseded`; one active Claim per claimant/Supplier pair; different claimants may compete; 30-day active horizon; terminal immutability | No deletion; **critical RLS** claimant/assigned-reviewer scoped; same-Supplier membership alone grants no access | reviewer assignment/decision, terminal timestamps, reason/method versions, restricted notes | Map `pending_review` to `submitted` absent assignment evidence; retain bounded immutable claimant/evidence snapshot; quarantine conflicts | Document 32 defines the approved workflow; the table remains Core Later, separately gated, and unselected. |
| `supplier_ownership_claim_evidence` — Remove/Merge; no physical table before a proven evidence/file need | Not applicable | Evidence remains a bounded private Claim payload; a typed file relation waits for FILE-001 | At most three descriptors; controlled kind/source/method/version; allowlisted HTTPS only where approved; submitted evidence immutable | Inherits Claim restriction/retention; never public | review verification is write-once and separate from claimant-submitted evidence | Preserve current type/summary/links as bounded descriptors; never invent a file object, verification, or legal authority | Reconsider a child table only after query, multi-file, custody, retention, and FILE-001 requirements justify it. |
| `categories` — canonical taxonomy | UUID or stable text `id`; `firebase_legacy_id` if category docs later exist | optional parent category | Unique canonical code; active/status checks | Soft archive; RLS authenticated/public-read decision | taxonomy approval and ordering | Seed only from reviewed taxonomy source; do not derive blindly from every Supplier string | Are current category strings codes, labels, or mixed historical values? |
| `supplier_category_assignments` — reviewed Supplier/category classification | UUID PK; normalized children use canonical category code plus assignment role as the deterministic source child key | Supplier profile, category, provider-neutral reviewer/actors | Unique active Supplier/category regardless of role/source; at most one active primary; unique active Supplier position; bounded lifecycle/provenance checks | Close as superseded/archived rather than normal delete; base row non-public and future projections field-minimized | role, lifecycle, review, source/mapping versions and actors | Resolve `categories[]`/`subcategories[]` only through reviewed exact/normalized/alias/manual rules; never infer primary from array order; ambiguous, broad-only, global `other`, unmapped, or rejected values create no assignment row | Option B is approved for a future empty local-only foundation. Later mapping/transformation artifacts remain required; aliases are not a prerequisite for empty DDL but are required before any alias-based transformation. |
| `supplier_capabilities` — reviewed indicative controlled/custom Supplier abilities | UUID PK; controlled child key uses kind/code/category scope; custom child key uses normalizer version plus normalized digest/category scope | Supplier profile; optional category; provider-neutral reviewer/actors | Mutually exclusive controlled/custom shape; unique active controlled/custom semantic target; unique active Supplier position; bounded lifecycle/provenance | Supersede/archive rather than normal delete; base row non-public; custom anonymous exposure separately approved | activation, moderation, provenance, mapping/review and actor fields | Route reviewed operational/service/documentary/experience `capabilityTags[]`; route `coverageAreas.imports_outside_iraq` here and nowhere geographic; payment/currency-like tags route to payment review; `official_invoice` is documentary capability; non-category free text remains evidence until reviewed | The document 28 capability contract is approved: category scope is optional, public projection is approved-active-labels-only, and `import_only` remains unresolved/excluded rather than equivalent to `imports_outside_iraq`. Exact vocabulary/DDL, mapping, and data work remain separately gated. |
| `supplier_payment_options` — reviewed indicative method/currency/credit/advance profile assertions | UUID PK; deterministic child key uses option type/code plus type-specific semantic parameters | Supplier profile; provider-neutral reviewer/actors | Mutually exclusive type-specific shape; credit days 1-365; active semantic uniqueness; explicit no-credit excludes positive credit; unique active Supplier position | Supersede/archive rather than normal delete; base row non-public; no client projection approved | activation, provenance, review evidence and actors recorded; authority remains future trusted/security work | Split `paymentOptions[]` into methods/currencies; map credit fields only when source evidence proves approved `invoice_date` or `delivery_acceptance_date`; route `official_invoice` to capability; preserve cheque/LC/advance unknowns, other start values, and contradictions for review | Merged PR #78 implements the approved empty ninth-slice foundation only. `official_invoice` remains excluded and routes only to documentary capability; mapping/data, RLS/access, trusted activation, RFQ-003, and BILL-001 remain separate. |
| `supplier_contacts` - one reviewed Supplier-associated phone/email/website endpoint | UUID PK; normalized children use deterministic source field/ordinal plus channel/scope outcome through internal migration mappings | Supplier profile; optional same-Supplier physical location; provider-neutral reviewer/verifier/actors | Mutually exclusive channel shapes; `company|named_person|unspecified` subject separate from phone kind; same-Supplier physical-location composite FK; unique active Supplier/scope/subject/channel/endpoint; unique active preference rank per scope/purpose | Personal/potentially personal; base row revoked/non-public; named-person and uncertain-subject values restricted; immediate deactivation and policy-driven erasure | normalization, verification, legal-basis/disclosure classification, review/lifecycle, provenance, ranking, and actors require trusted authority | Treat `phones[]`, email, and website as independent candidates; attach branch phones only to exact mapped physical rows; never attach `contactPerson`/role to channels, infer company ownership/purpose/consent, or promote current normalized values without reviewed evidence | Document 30 is Owner-approved, SUP-005 is Resolved, and the empty foundation plus minimal supporting location uniqueness object is the proposed tenth slice. Exact SQL/pgTAP, rows, mapping, trusted commands, RLS/projections, and data work remain separately gated. |
| `supplier_locations` — typed physical-presence and service-coverage assertions | UUID `id`; normalized children use deterministic source child keys/ordinals through the internal migration mappings rather than a direct root legacy ID | Supplier profile; conditional administrative area; nullable provider-neutral reviewer/actor profiles | Class/kind/target shape checks; one active headquarters; unique active mapped coverage target and `all_iraq`; unique active Supplier/class/kind position; physical rows are not unique by area | Archive rather than normal hard delete; base row non-public; address/map evidence and mapping metadata require later field-minimized projection review | mapping/review/source provenance, lifecycle, and actor fields | Top-level location fields and `branches[]` become ordered physical rows; additional recognized `governorates[]` values may become `unspecified_presence`; `all_iraq` becomes national coverage; ambiguous coverage stays non-mapped; `imports_outside_iraq` produces no location row | Exact mapping manifest, projection, contact transformation, RFQ use, and geographic search remain later reviewed work. |
| `supplier_catalog_items` — products/services from `supplierProducts` | UUID `id`; unique `firebase_legacy_id` | Supplier profile, owner user, category, optional uploaded file | Type/status checks; unique legacy ID; ownership agreement | Soft archive; high RLS | moderation/status/media readiness | Map current product/service fields; retain bilingual text; media remains absent until Storage phase | Product variants/pricing remain out of initial scope? |
| `supplier_submissions` — proposed Supplier data | UUID `id`; unique `firebase_legacy_id` from `supplierSubmissions` | submitter, import batch, reviewer, approved Supplier | Status transition checks; unique import idempotency key; row/source checks | No hard delete after review; **high RLS** submitter/admin | decision, duplicate override, counts-for-access, approved target | Normalize searchable columns and retain restricted immutable `supplier_payload_snapshot`/duplicate snapshot | Which draft fields should be relational before approval versus snapshot-only? |
| `supplier_import_batches` — import metadata | UUID/text `id`; unique `firebase_legacy_id` from `supplierImportBatches` | importing user | Accepted rows ≤50; file size ≤204800; allowed role/source/status | Retain for audit; high RLS importer/admin | completion/summary counts | Map metadata only; never store raw workbook; preserve original safe filename | Retention period for import metadata? |
| `duplicate_fingerprints` — canonical duplicate prevention | UUID `id`; legacy IDs from `supplierCanonicalUniqueness`/duplicate indexes | Supplier profile, submission | Unique `(kind, fingerprint_hash)`; hash format/kind checks | No soft delete while Supplier active; **server-only RLS** | hashes, collision/conflict state, algorithm version | Recompute with versioned normalizer; retain legacy hash/variant mapping; never expose contact fingerprint inputs | Backfill strategy for 480 historical Suppliers and algorithm-version migration? |
| `supplier_search_identity` — normalized search/dedup projection | Supplier profile PK; legacy `supplierDuplicateIndex` ID | Supplier profile | Unique Supplier; bounded normalized arrays; version check | Rebuilt/derived; read only to trusted search path | normalized phones/email/URLs/search variants | Transform denormalized duplicate index; keep sensitive columns out of public API | PostgreSQL full-text/trigram versus dedicated search service? |

## Access, trials, and referrals

| Proposed table / purpose | Primary key and Firebase legacy field | Main foreign keys | Important unique and check constraints | Soft delete and RLS sensitivity | Trusted-server-only fields | Expected transformation | Open questions |
|---|---|---|---|---|---|---|---|
| `access_grants` — immutable access periods from `accessGrants` | UUID `id`; unique `firebase_legacy_id` | user, source event/credit, actor | Positive days; valid date range; deterministic trial uniqueness | Immutable, never delete; **high RLS** self/admin | grant decision/source/audit reference | Convert timestamps and preserve trial/contribution IDs | Should access be computed from grants or also materialized on user? |
| `access_credits` — earned/applied credit ledger | UUID `id`; unique `firebase_legacy_id` from `accessCredits` | user, grant, contribution | Non-negative counts/days; allowed source/status; unique deterministic source event | Immutable corrections via compensating row; high RLS | applied state, expiry deltas | Normalize current trial/contribution payload variants | Can pending credits expire or be revoked? |
| `contribution_events` — referrals/submissions/reviews ledger from `contributionLogs` | UUID `id`; unique `firebase_legacy_id` | user, submission, Supplier, review | Allowed event type; non-negative points; unique source event | Immutable; high RLS self/admin | reward eligibility and points | Map contribution logs; distinguish referral/incentive source from generic contribution | Product naming: referral, contribution, or both? |
| `entitlements` — effective access state | UUID `id`; no current standalone collection | user, subscription/access grant | Non-overlapping active entitlement by type; valid dates | Temporal history, no destructive overwrite; **critical RLS** | effective status/reason/source | Initially derive from grants/user access fields; later unify billing and trials | Which feature capabilities belong to each entitlement? |

## RFQ and quotation aggregate

| Proposed table / purpose | Primary key and Firebase legacy field | Main foreign keys | Important unique and check constraints | Soft delete and RLS sensitivity | Trusted-server-only fields | Expected transformation | Open questions |
|---|---|---|---|---|---|---|---|
| `rfqs` — RFQ header/lifecycle | UUID `id`; unique `firebase_legacy_id` from `rfqs` | buyer user, category | Status transition checks; closing time; published RFQ needs recipients; HTTPS reference-link limits | Cancel/close, never hard-delete after publication; **critical RLS** | publication/closure actors and transition version | Move header fields; retain attachment placeholder and legacy timestamps/status | Multi-item RFQ UX timing and future award semantics? |
| `rfq_items` — requested line items | UUID `id`; legacy `${rfqId}:1` for current single-item RFQs | RFQ, optional category/product | Unique `(rfq_id, line_number)`; positive quantity; unit/other checks | Follow RFQ retention; same RLS | normalized technical/commercial fields | Create one item from current title/description/quantity/unit; preserve original header during compatibility period | Which current header fields move to item versus remain RFQ-wide? |
| `rfq_recipients` — target Suppliers | UUID `id`; legacy pair from `recipientIds[]` | RFQ, Supplier profile, ownership snapshot | Unique `(rfq_id, supplier_profile_id)`; eligible-at-publication evidence | Never silently remove after publication; **critical RLS** | eligibility snapshot, invited owner UID, event state | Explode recipient array; capture owner/eligibility snapshot during migration | How to represent Supplier ownership changes after invitation? |
| `quotations` — canonical current response from `rfqResponses` | UUID `id`; unique `firebase_legacy_id` `${rfqId}_${supplierUid}` | RFQ, recipient, Supplier profile, Supplier user | One quotation per RFQ/Supplier profile; allowed status; current revision pointer integrity | Withdraw/status change, no hard delete; **critical RLS** | revision number/current revision, first submission | Map canonical response and verify deterministic ID/ownership agreement | Should uniqueness use Supplier profile only or profile+user? |
| `quotation_items` — current commercial line values | UUID `id`; legacy `${responseId}:1` | quotation, RFQ item | Unique `(quotation_id, rfq_item_id)`; non-negative price; currency/unit checks | Versioned through revisions; critical RLS | normalized totals/compliance | Create one item from current response-level commercial fields | How will tax, freight, alternates, and partial bids be modeled? |
| `quotation_revisions` — immutable revision header from `rfqResponseRevisions` | UUID `id`; unique `firebase_legacy_id` `${responseId}_vN` | quotation, RFQ, buyer, Supplier profile/user, prior revision | Unique `(quotation_id, revision_number)`; monotonic positive revision; immutable | Never update/delete; **critical RLS** | created_by, change type, event linkage | Map every revision; synthesize V1 only under an approved reconciliation plan for legacy records | Are any legacy canonical responses missing V1 outside controlled data? |
| `quotation_revision_items` — immutable commercial snapshot | UUID `id`; legacy `${revisionId}:1` | quotation revision, RFQ item | Unique `(revision_id, rfq_item_id)`; same commercial checks | Immutable; critical RLS | snapshot hashes/diff metadata | Split each revision payload into item snapshot while preserving raw restricted snapshot/hash | Exact comparison/currency rounding rules? |
| `quotation_decisions` — award/shortlist outcome (future scope) | UUID `id`; no Firebase legacy ID yet | RFQ, quotation, buyer, Supplier | Allowed decision enum; one current decision per quotation/RFQ; decision history immutable | Never hard-delete; **critical RLS** | buyer actor/reason/event; Supplier read-only projection | No migration rows unless later feature exists | Business/legal wording, reversibility, and notification rules require product decision |

- **[Verified current fact]** Current first submission creates quotation, V1 revision, response event, and Buyer notification atomically. Current updates use a transaction, compare material commercial fields, allocate the next deterministic revision/event/notification, and treat identical resubmission as a no-op. Sources: `src/services/workspace.ts`, `src/utils/rfqLifecycle.ts`, and RFQ Emulator tests.
- **[Future plan]** The PostgreSQL command boundary must recreate that entire invariant; direct writes to `quotations` or revision tables are not acceptable.

## Conversations, messages, and notifications

| Proposed table / purpose | Primary key and Firebase legacy field | Main foreign keys | Important unique and check constraints | Soft delete and RLS sensitivity | Trusted-server-only fields | Expected transformation | Open questions |
|---|---|---|---|---|---|---|---|
| `conversations` — conversation context | UUID `id`; unique `firebase_legacy_id` | optional RFQ and Supplier profile | Context requires legitimate buyer/Supplier relationship; deterministic uniqueness by context | Archive only; **critical privacy RLS** | context eligibility snapshot, lifecycle | Map header and last-message summary; remove participant map/array to child rows | Can Admin/Owner ever join, and under what audited support policy? |
| `conversation_participants` — membership/labels | UUID `id`; legacy conversation+UID pair | conversation, application user | Unique `(conversation_id, user_id)`; participant role/status checks | Temporal removal, no destructive history; **critical RLS** | membership grant/revoke actor | Explode `participantIds[]`; map `participantLabels` as snapshot only | Should display labels remain snapshots or always resolve current profiles? |
| `messages` — private message body | UUID `id`; unique `firebase_legacy_id` | conversation, sender, optional uploaded file | Sender must be active participant; bounded non-empty body; created-at immutability | Tombstone only if product/legal policy permits; **critical privacy RLS** | moderation/tombstone metadata | Map message documents; attachments currently absent/pending | Retention, moderation, export, and deletion policy? |
| `message_receipts` — normalized `readBy[]` | Composite/UUID PK; legacy message+UID pair | message, participant user | Unique `(message_id, user_id)`; user can add only own receipt | Immutable receipt; **critical RLS** | delivered/read timestamps beyond current scope | Explode `readBy[]`; preserve only valid participants; sender/read semantics reviewed | Need delivered/seen distinction and per-device state? |
| `notifications` — user notification inbox | UUID or deterministic text PK; unique `firebase_legacy_id` | recipient, actor, domain event, referenced entity | Unique deterministic event/recipient key; allowed type/reference; recipient immutable | Read/tombstone; **high RLS** recipient only | creation payload, event link, immutable routing | Map current bilingual snapshots and read state; validate deterministic associations | Retention and localization snapshot versus template rendering? |

## Audit, deterministic events, idempotency, files, and billing

| Proposed table / purpose | Primary key and Firebase legacy field | Main foreign keys | Important unique and check constraints | Soft delete and RLS sensitivity | Trusted-server-only fields | Expected transformation | Open questions |
|---|---|---|---|---|---|---|---|
| `internal.audit_logs` — immutable security/business audit | UUID `id`; optional unique legacy source identity from `auditLogs` | provider-neutral actor user where applicable; correlation references need no forward FK | Immutable registered action/target/outcome; bounded reason and minimized versioned change evidence | Append-only; fully revoked non-exposed base; no ordinary update/delete; correction by linked row | all write/evidence fields | Map only reconciled structured evidence; sanitize/minimize details; preserve safe source hashes/manifests | Owner-approved document 35 resolves AUD-001 and selects the empty structural foundation; exact retention/legal/access/enforcement decisions remain required before real rows but do not block empty local DDL. |
| `domain_events` — deterministic RFQ/ownership/submission events | UUID/text `id`; source collection+document legacy key | aggregate IDs, actor, causation event | Unique `(source_system, legacy_collection, legacy_id)` and domain idempotency key; allowed type | Immutable; **server-only RLS** | payload/version/causation/processed state | Union `rfqPublishEvents`, `rfqResponseEvents`, `supplierOwnershipEvents`; preserve exact IDs/types | One generic table versus bounded-context event tables? |
| `idempotency_records` — command replay protection | Text/UUID `id`; legacy claim-request hash/import key | actor, command result/aggregate | Unique `(scope, idempotency_key_hash)`; payload hash binding; expiry checks | TTL/partition cleanup after policy; **server-only RLS** | raw key never stored, result binding, expiry | Map `supplierOwnershipClaimRequests`; register future commands with scoped hashes | Retention horizon per command and safe replay response schema? |
| `uploaded_files` — file metadata independent of provider | UUID `id`; `firebase_legacy_id` from `supplierDocuments` or future record | owner user, Supplier/RFQ/message/claim entities | Unique provider object key; size >0; allowed MIME/purpose/status; checksum required before ready | Soft quarantine/delete state; **critical RLS** | bucket/key, checksum, scan, verification, signed-URL state | Current Supplier document metadata becomes metadata-only row; no object copy exists now | Bucket split, maximum sizes, malware scanning, retention, legal holds? |
| `subscriptions` — commercial subscription (future) | UUID `id`; no Firebase legacy ID | organization/user, Stripe customer/subscription refs | Unique provider subscription ID; allowed lifecycle; period checks | Cancel/expire, never erase billing history; **critical RLS** | provider status, price/product IDs | No current migration; design only after Stripe sandbox phase | Buyer versus Supplier payer, organization billing, plan model? |
| `billing_events` — Stripe webhook/event ledger (future) | UUID/text `id`; no Firebase legacy ID | subscription/customer/organization | Unique Stripe event ID; payload hash; processed state; event timestamp | Immutable; **server-only RLS** | restricted raw payload, signature verification result, processing errors | No current migration; ingest only through verified webhook path | Payload retention and replay/dead-letter process? |

## Secondary current collections

| Proposed table / purpose | Primary key and Firebase legacy field | Main foreign keys | Important unique and check constraints | Soft delete and RLS sensitivity | Trusted-server-only fields | Expected transformation | Open questions |
|---|---|---|---|---|---|---|---|
| `favorite_suppliers` from `favorites` | UUID; unique legacy ID | user, Supplier | Unique `(user_id, supplier_profile_id)` | Hard delete acceptable; self-only RLS | none | Map denormalized Supplier name only as optional snapshot | Keep snapshot or always join? |
| `supplier_reviews` from `reviews` | UUID; unique legacy ID | Supplier, reviewer | Rating ranges; status; one-review policy if any | Moderate/tombstone; high RLS | moderation decision | Map rating dimensions/tags; retain comments restricted | Review multiplicity and public visibility? |
| `supplier_feedback` from `supplierFeedback` | UUID; unique legacy ID | Supplier, submitter, reviewer | Type/status and bounded text | Retain audit; high RLS | admin decision | Map current feedback payload | Should corrections produce separate audited Supplier change requests? |
| `material_terms` from `materialTerms` | UUID/text; unique legacy ID | category | Unique normalized term; status checks | Soft archive; authenticated read | moderation | Normalize synonym/brand/standard arrays later if query need proves it | PostgreSQL FTS/trigram strategy? |
| `term_suggestions` from `termSuggestions` | UUID/text; unique legacy ID | material term, reviewer | Unique normalized suggestion; non-negative count | Retain/merge state; admin RLS | moderation and merge link | Map examples/sources as child rows or restricted JSON | Required retention for search examples? |
| `platform_settings` from `settings`/`publicConfig` | Text key; unique legacy ID | updater | Schema-versioned value checks | Version rather than delete; mixed public/admin RLS | protected operational settings | Split public registration config from protected settings | Which settings warrant typed tables rather than JSON? |
| `content_pages` from `contentPages` | UUID/text; unique legacy ID/slug | updater | Unique slug; draft/published; order checks | Soft archive; public published read | publication actor | Map bilingual content/SEO fields | CMS version history required? |

## Migration transformation rules

1. **[Future plan]** Export a bounded, approved snapshot with source commit/deployment/rules/index metadata and collection counts; never use application reads as an implicit export.
2. **[Future plan]** Preserve every Firestore document ID in a unique legacy column before creating relationships.
3. **[Future plan]** Convert references in a second deterministic pass and fail on orphan, ambiguity, or two-sided ownership mismatch.
4. **[Future plan]** Explode arrays into relation tables where they affect authorization, uniqueness, querying, or referential integrity; retain ordered snapshots where audit fidelity requires it.
5. **[Future plan]** Recompute normalized fields and fingerprints with a versioned algorithm, compare to legacy values, and quarantine differences for review rather than overwriting silently.
6. **[Future plan]** Verify immutable revisions/events/audits by counts, unique legacy IDs, per-aggregate sequences, and deterministic hashes.
7. **[Future plan]** Keep imported Production rows read-only until mapping, constraints, RLS, and reconciliation tests pass in rehearsal.
