# Mujahiz IQ PostgreSQL Schema Decision Register

Status: Design register with local SQL-slice evidence
Baseline: `main` at `206f7daa524228abfa83793c39a03045491f1316`
Companion design: `09_POSTGRESQL_SCHEMA_DESIGN.md`

## 1. Use of this register

- `Recommended` means the schema design adopts the direction, but it is not implemented.
- `Open` requires the named approver before the stated phase.
- `Resolved` means the named decision is implemented for the explicitly recorded scope and evidence.
- `Partially implemented` means the declarative schema contract exists but a named runtime or hosted gate remains.
- `Deferred` keeps a compatibility seam but excludes implementation from the initial migration.
- `Rejected` records an alternative that must not reappear without new evidence.
- Changing a resolved recommendation requires an ADR or an explicit change to this register and its dependent design sections.
- No row authorizes SQL, RLS, hosted Supabase work, Firebase changes, data movement, or Production access.
### Second SQL-slice implementation evidence

Migration 20260804000200_provider_neutral_identity_foundation.sql implements only public.user_profiles and internal.identity_provider_links on the local implementation branch. It applies the DB-001 UUIDv4 convention to both keys and the ID-002 provider-subject separation/active uniqueness boundary, with synthetic pgTAP and catalog evidence recorded in 14_SECOND_SQL_SLICE_IMPLEMENTATION_EVIDENCE.md. ID-001 remains **Open**: the local tables do not select an Auth authority, validate provider tokens, integrate Firebase or Supabase Auth, expose browser access, or create verified-only benefits. No other Open decision is resolved by this implementation.

## 2. Decisions

| ID | Topic | Status | Decision / recommendation | Rationale and evidence | Alternatives considered | Schema and delivery impact | Required approver | Must resolve before phase | Resolution date | Evidence / reference | Resolved state |
|---|---|---|---|---|---|---|---|---|---|---|---|
| DB-001 | Primary keys | Resolved | Use database-generated UUIDv4 through `pg_catalog.gen_random_uuid()` for the local SQL slices. UUIDv7 remains preferred only if a later approved environment exposes a stable reviewed mechanism. | Local Supabase CLI `2.111.0` runs PostgreSQL `17.6`; catalog inspection found no UUIDv7 function and found built-in `pg_catalog.gen_random_uuid()` plus installed UUIDv4 mechanisms. Public opaque keys and migration independence remain required. | Custom UUIDv7 function; third-party extension; JavaScript/client-generated UUID; identity/bigint; legacy text keys | Every table in the implemented local SQL slices uses the same database UUIDv4 default. Hosted compatibility remains a later validation gate and relationships do not depend on UUID version. | Technical owner | First migration SQL PR | 2026-08-04 | SQL foundation document; migration `20260804000136`; local PostgreSQL capability query | Resolved for the local first SQL slice; hosted version/capability not claimed |
| DB-002 | Status representation | Recommended | Use bounded text with checks and trusted transition commands. | Status sets will evolve across staged migration; repository currently uses string unions. | PostgreSQL enums; lookup tables | Avoids enum migration coupling; every status needs a constraint and transition test. | Technical owner | First migration SQL PR | Not resolved | Design `B` | Design direction adopted; implementation pending |
| DB-003 | Soft deletion | Recommended | Use explicit archive/close/revoke/tombstone fields only for domains that require history. | Universal soft delete obscures domain semantics and risks leaking archived rows. | Universal `deleted_at`; hard delete only | Per-table retention and query predicates are mandatory. | Technical + product owner | Per-domain SQL PR | Not resolved | Design `F` | Design direction adopted; implementation pending |
| DB-004 | Money and quantity | Recommended | Use exact numerics, prohibit floating point, record ISO currency per revision, and canonicalize to scale 4 for no-op hashing. | Current `price` is not precise enough for future comparison/awards. | Integer minor units; float; RFQ-only currency | RFQ-003 still gates legacy meaning; new commands use the documented exact contract. | Finance/product owner | RFQ data transformation | Not resolved | Design `B` and canonical no-op hash | Design direction adopted; implementation pending |
| DB-005 | JSONB boundary | Recommended | Permit versioned bounded snapshots/metadata only; keep queryable identities, states, relationships, lines, recipients, and contacts relational. | Firestore maps/arrays require integrity and selective access. | Document-shaped JSONB port; fully decomposed metadata | JSONB keys need schemas, size limits, and no secret/record dumps. | Technical owner | First SQL PR per domain | Not resolved | Design `B` | Design direction adopted; implementation pending |
| ID-001 | Authentication authority | Open | Keep Firebase Auth as initial verification/disablement authority through provider links and a two-stage lifecycle: unverified registration/profile bootstrap, then trusted verification refresh with idempotent first-verified trial/access ledger work; design provider-neutral user PKs without selecting the future Auth authority. | Current registration can bootstrap an unverified identity, while Firebase remains authoritative for verification during the hybrid phase; Auth migration is not required for Firestore data migration and increases cutover risk. | Immediate Supabase Auth cutover; browser-authoritative verification; permanent Firebase Auth | Requires secure Firebase token/provider-state validation, correction/loss reconciliation, and a later identity migration ADR; repeated refresh cannot duplicate benefits. | Owner/security | Identity migration phase | Not resolved | Design identity conclusions, `D1` and Stage 1/2 commands in `G` | Unresolved approval gate |
| ID-002 | Firebase UID mapping | Recommended | Preserve Firebase UID as unique active provider subject, never as relational PK. | Current documents and ownership rely on UID; provider neutrality is required. | UUID only with external mapping file; UID as PK | Identity reconciliation and active-row uniqueness are migration gates. | Technical/security | User transformation | Not resolved | Design `B` and `D1` | Design direction adopted; implementation pending |
| ID-003 | Platform roles | Recommended | Use temporal trusted Owner/Admin assignments, one effective active role absent reviewed exception, Owner/Admin incompatibility, and the complete usable-Owner predicate across active profile, approved account status, compatible context, active Firebase link, Firebase-verified/non-disabled identity, active Owner assignment, valid administration access, and repository-backed security eligibility. Use a non-expiring trusted role-backed administration grant while usable Owner authority is held. | Role, account, identity, verification, access, and background corrections can each remove operational authority; every path must serialize and must not strand administration. | Role on profile; unrestricted many-role set; organization roles reused; allow ordinary access expiry to remove Owner authority | Direct commands and expiry/correction jobs lock/re-evaluate the same candidate Owner set, fail closed below one usable Owner, audit/security-record the outcome, and use explicit compensation/recovery. No new decision ID is required. | Owner/security | RLS design | Not resolved | Design identity conclusions, `D1`, complete Owner safety contract and commands in `G` | Design direction adopted; implementation pending |
| ORG-001 | Organization model and nullable bootstrap | Open | Keep organization linkage nullable/deferred; migrate users and Suppliers without fabricated organizations; place future RFQs under organizations only after approved evidence-based bootstrap. | No current organization collection exists and free text does not prove a legal entity. | User-owned RFQs indefinitely; auto-create about 480 Supplier organizations | Initial RFQs retain Buyer creator/context; Supplier profiles remain independent; organization table is Future-Compatible and uncreated initially. | Product owner | RFQ migration | Not resolved | Design phase manifest, initial slice and identity conclusions | Unresolved approval gate |
| ORG-002 | Organization membership roles and bootstrap | Open | After ORG-001 approval, start with minimal temporal Buyer owner/admin/member roles and one active user/organization membership; never infer membership from free text. | Avoids prematurely encoding a complex procurement hierarchy or reusing platform roles. | Platform roles reused; full configurable RBAC; automatic membership import | Affects invitations, RLS, decisions and billing; current users preserve context without membership rows. | Product/security | Organization RLS design | Not resolved | Design `D1` and organization-bootstrap command | Unresolved approval gate |
| SUP-001 | Supplier ownership versus membership | Recommended | Keep verified primary ownership temporal and separate from future operational memberships; current profiles require neither organization nor membership. Retain the authoritative Claim design but classify `supplier_ownership_claims` Core Later, with bounded evidence on the Claim rather than a separate evidence table. | Current `accountOwnerId` and user backlink express authority only when they agree; future multi-user access has different semantics. Production has zero Claim rows, Claim is GitHub-only/undeployed, and the initial slice does not require it. | Owner UID on profile; membership alone; empty Phase-1 Claim table; separate Claim-evidence table | Migration creates ownership only on two-way agreement; approval/transfer/revoke retain temporal history. Core Phase 1 has 36 concepts and Core Later has 10. | Product/security | Supplier SQL/RLS design | Not resolved | Design phase manifest, Supplier conclusions, `D2` and commands | Design direction adopted; implementation pending |
| SUP-002 | Supplier multi-user access | Recommended | Permit future role-bearing members with one active Supplier/user membership regardless of role; initially keep one active primary owner and one profile per owner. | Supports team scale without weakening current uniqueness guardrails. | One owner forever; immediate owner-to-many | Membership table is Future-Compatible and uncreated initially; new roles/actions remain disabled. | Product/security | Membership feature phase | Not resolved | Phase manifest and `D2` | Design direction adopted; implementation pending |
| SUP-003 | Category hierarchy | Open | Use stable category codes, parent links, paired Arabic/English launch labels, and alias mapping; merge `category_translations` initially. | Legacy arrays mix categories, subcategories, and free-text capabilities. | Flat labels; fixed level tables; separate translations now | Requires governed mapping and exception list before Supplier migration. | Product/data owner | Supplier transformation | Not resolved | Design `B`, phase manifest and `D5` | Unresolved approval gate |
| SUP-004 | Address and service-coverage normalization | Open | Separate physical locations from service coverage; require area FK only for mapped administrative values and use reviewed bounded codes for `all_iraq` and `imports_outside_iraq`. | Coverage contains non-administrative values and current address standards are incomplete. | Require area for every row; free text only; full locality hierarchy now | Original text/mapping status and exceptions are preserved; invalid values are never invented. | Product/data owner | Supplier transformation | Not resolved | Design Supplier conclusions and `supplier_locations` | Unresolved approval gate |
| SUP-005 | Contact model and audience projection | Recommended | Normalize contact channels with visibility/order/verification and expose Supplier data only through approved audience-specific security-invoker views or RPC projections. | Base-row RLS cannot provide column security and arrays/nested branches cannot express field-level privacy. | Public base tables; contacts JSONB; table per channel | Base Supplier tables are non-public; anonymous directory exposure is a future approval and private fields never enter a public projection. | Product/security | Supplier SQL/RLS design | Not resolved | Design RLS boundary and `D2` | Design direction adopted; implementation pending |
| SUP-006 | Product catalog depth | Deferred | Keep Catalog Lite compatibility as Core Later; defer variants, inventory, and transactional pricing. | Current collection is empty and procurement does not require full commerce modeling. | Full catalog now; omit products entirely | Product table is not in Core Phase 1; files remain gated. | Product owner | Product feature phase | Not resolved | Phase manifest and `D2` | Deferred by design |
| SUP-007 | Exact and fuzzy duplicate prevention | Recommended | Use versioned protected exact digests with active uniqueness and atomic pending promotion, plus a versioned bounded fuzzy-candidate projection and human review queue. | Current guard collections encode critical concurrency invariants while name similarity requires supporting evidence rather than exact equality. | Public hashes; fuzzy-only; exact-only; automatic merge; literal three-table port | Key rotation/normalizer/scorer versions are recorded; Dice or approved equivalent uses name, area/category and supporting evidence; no automatic merge/delete. | Security/technical | Supplier migration SQL PR | Not resolved | Design duplicate decision, `D2` and duplicate command | Design direction adopted; implementation pending |
| RFQ-001 | RFQ party and recipients | Recommended | Preserve transitional Buyer creator/context while organization linkage is Open; publication creates immutable Supplier recipient rows and each recipient is the sole quotation authorization anchor. | UID arrays and duplicated RFQ/Supplier quotation fields cannot enforce party integrity. | Require fabricated Buyer organization; Buyer user plus UID arrays; duplicated quotation parties | Recipient uniqueness and eligibility are publication gates and migration reconciliation targets. | Product/security | RFQ SQL/RLS design | Not resolved | Design RFQ conclusions, `D3` and commands | Design direction adopted; implementation pending |
| RFQ-002 | Quotation revision and no-op model | Recommended | Use one quotation per recipient with immutable revisions/items, temporarily nullable current pointer set in the same transaction, composite same-parent integrity, and no `quotation_items` table. | Existing V1/V2/events require immutable history; prior model had circular insertion and competing projections. | Mutable response; JSONB revisions only; required circular pointer; current-item table | Revision number is unique per quotation; canonical hash makes identical submissions true no-ops with no audit/event/notice. | Product/technical | RFQ SQL PR | Not resolved | Design RFQ conclusions, no-op hash, `D3` and command | Design direction adopted; implementation pending |
| RFQ-003 | Legacy price and currency | Open | Define unit/line/total semantics, numeric scale, tax/freight, and currency before transforming response `price`. | Current evidence is insufficient to interpret commercial amounts safely. | Preserve untyped value; infer currency from locale | Blocks authoritative revision-item transformation and comparison totals; no-op normalization applies only after meaning is approved. | Finance/product owner | RFQ data transformation | Not resolved | Design `D3` and canonical no-op hash | Unresolved approval gate |
| RFQ-004 | Award and value measurement | Deferred | Preserve Future-Compatible immutable award/no-award decision and approved value-baseline concepts; do not create or enable yet. | Future product value must be measurable without mutating quotation history. | Derive award from status; omit value model | Approval thresholds, partial award, disclosure, and savings method remain open. | Product/finance owner | Award feature phase | Not resolved | Phase manifest and `D3` | Deferred by design |
| MSG-001 | Conversation party and temporal access | Recommended | Use temporal participants with inclusive `valid_from` and exclusive `valid_until`; reads are limited to messages inside that interval. | Current participant arrays cannot stop access to later messages or protect prior private history on ownership change. | Current-membership-only check; inherited full history; generic group chat | Revocation forbids later messages; normal historical access is interval-bounded; new owners receive no old messages; RFQ close/cancel closes write epochs. | Product/security | Messaging SQL/RLS design | Not resolved | Design messaging conclusions, `E` and conversation command | Design direction adopted; implementation pending |
| MSG-002 | Message edit/delete | Open | Launch the later relational messaging phase with immutable messages; evaluate trusted tombstone/moderation separately. | Current messages are immutable and private; deletion affects evidence and privacy rights. | User edit window; hard delete | Determines history, audit, attachment, export and exceptional quarantine rules. | Product/legal/security | Messaging SQL PR | Not resolved | Design messaging conclusions and `F` | Unresolved approval gate |
| MSG-003 | Notification retention/rendering | Open | Store a safe bilingual per-channel delivery snapshot linked to a domain event and expire by class; one worker alone materializes notifications. | Live rendering can drift; indefinite duplication increases exposure; dual creation paths duplicate rows. | Render live; template parameters only; command and worker both insert | Requires class-based retention and protected-notice policy; deterministic event/recipient/channel uniqueness and migration replay suppression are mandatory. | Product/security | Notification SQL PR | Not resolved | Design messaging conclusions, `D4` and fan-out command | Unresolved approval gate |
| SEARCH-001 | Search technology | Open | Start with PostgreSQL FTS; measure Arabic/English recall and add `pg_trgm` only if justified. | Current client search loads pages and scans keyword arrays; server-side ranked search is required. | Trigram first; external search service | Determines extensions, indexes, ranking tests, and operational complexity. | Technical/data owner | Directory query migration | Not resolved | Design `H` query matrix | Unresolved approval gate |
| CONTENT-001 | Bilingual content | Recommended | Use paired Arabic/English columns for bounded categories and transactional snapshots; row-per-locale remains for future governed pages where lifecycle differs. | Launch taxonomy has two required languages and does not justify `category_translations`; page translation lifecycle may differ. | JSONB locale maps; separate translation rows everywhere; paired columns everywhere | Locale fallback is explicit and no language is silently copied. | Product/content owner | Content SQL PR | Not resolved | Phase manifest and `D5` | Design direction adopted; implementation pending |
| FILE-001 | Object storage provider and custody | Open | Before approval, allow only strictly validated allowlisted HTTPS or preserved legacy references with source/provider metadata in typed attachment rows or bounded Claim evidence; include no `file_objects` column/FK. Keep future provider-neutral `file_objects`, creator/uploader provenance only, and parent-derived custody authorization. | Files/Storage are disabled; legacy references may lack size/MIME/hash; uploader identity, opaque keys, and permanent public URLs do not prove continuing domain access. | Firebase Storage; Supabase Storage; creator-owned files; public URLs; arbitrary external schemes | Controls later signing, scanning, lifecycle, region, orphan compensation and typed-parent authorization. An audited later migration may replace references with provider-neutral IDs. | Technical/security | Any upload enablement | Not resolved | Design file decision, pre-FILE-001 strategy, `D2`/`D3`/`D4`/`D6` and finalization command | Unresolved approval gate |
| FILE-002 | Upload launch state | Recommended | Keep uploads disabled until provider, validation, scanning, typed custody, access and retention decisions are implemented and tested; Phase-1 reference rows imply no Storage and private files are never permanent public URLs. | Repository exposes metadata/placeholders and controlled HTTPS links only. | Enable while migrating metadata | Prevents fake file rows/FKs, unsafe schemes/objects, public paths, or invented metadata; file tables remain uncreated initially. | Owner/security | Upload feature approval | Not resolved | Phase manifest, pre-FILE-001 strategy and `D6` | Design direction adopted; implementation pending |
| BILL-001 | Billing subject | Open | Prefer organization-owned billing/entitlements with explicit user fallback for legacy access. | Team procurement points toward organizations, while current grants are user-scoped and ORG decisions remain Open. | User-only billing; Supplier billing first | Affects customer mapping, entitlement joins, invoicing roles and migration; billing tables are uncreated initially. | Business/product owner | Billing design phase | Not resolved | Phase manifest and `D8` | Unresolved approval gate |
| BILL-002 | Billing event integrity | Recommended | Authenticate/deduplicate provider events into an immutable ledger, then derive subscription and entitlement projections. | Billing cannot trust browser writes or webhook retries. | Mutate subscription directly from webhook; client state | Provider choice/raw-event retention remain open; no payment credentials stored. | Finance/security | Billing implementation | Not resolved | Design `D8` | Design direction adopted; implementation pending |
| AUD-001 | Audit/security retention | Open | Approve class-based duration, minimization, legal hold and pseudonymization rules. | Current audit volume is significant and data classes have different risks. | Universal duration; indefinite retention | Blocks final deletion jobs, partitions and migration selection. | Owner/legal/security | Audit migration | Not resolved | Design `F` and internal schema boundary | Unresolved approval gate |
| RES-001 | Data residency | Open | Select region/provider after legal, Supplier-data, latency, backup and recovery review. | Hosted Supabase environment is not verified or authorized. | Default region; self-hosting | Blocks hosted Production project creation and data movement. | Owner/legal/security | Hosted environment creation | Not resolved | Current baseline and design scope | Unresolved approval gate |
| MIG-001 | Legacy identifiers and zero-to-many trace | Partially implemented | Preserve root Firestore IDs as source identities and use physically distinct disposition, target-binding, and merge-member relations. Every ordinary mapping has exactly one semantic child key or deterministic ordinal; active logical-slot uniqueness excludes target UUID; active reverse-target uniqueness spans ordinary and reviewed merge targets; merge contributors use a separate member relation. | Rollback, replay, discard/quarantine, normalized child expansion, non-forking mappings, unambiguous reverse trace, and content reconciliation require separate source outcome/target facts and retained superseded history. | Reuse IDs as PK; one mapping only; include target UUID in slot uniqueness; separate target tables with an unenforced cross-table reverse constraint; relax reverse uniqueness for merges; fake no-target UUID; discard superseded mappings | The local `internal` schema implements batch/version-coupled dispositions, shared target bindings/merge-group headers, target-qualified merge members, and database-generated successor lineage IDs. Declarative constraints cover active parent coupling, child slots, reverse targets, replay identity, bounded versioned metadata, passed-evidence consistency, contributor isolation, and reasoned same-lineage supersession. Runtime locking, identical-replay lookup, minimum/complete merge-member validation, atomic graph supersession, reconciliation orchestration, owner-level immutability, and rollback execution remain Migration Engine/access-control work. | Technical owner | Migration Engine runtime / first data migration | 2026-08-04 (schema contract) | SQL foundation document; migration `20260804000136`; 60/60 local database assertions | Declarative schema contract implemented locally; runtime behavior remains open |
| MIG-002 | Environment strategy | Open | Use separate local, development, staging and Production projects with explicit environment gates. | Local stack is disposable; no hosted environment has been verified; Production writes need approval. | Shared hosted project; local plus Production only | Determines CI, secrets, project references, backups and promotion controls. | Owner/technical lead | Any hosted project creation | Not resolved | Current baseline, design scope, and SQL foundation document | Unresolved approval gate; this local slice provides no hosted-environment evidence |
| SEC-001 | RLS delivery | Recommended | Design/review RLS separately after schema approval, including temporal-message and cross-party negative tests; base Supplier tables are non-public. | This task is schema documentation and RLS cannot supply column security alone. | Combine schema/RLS; expose base tables | No client-accessible migration ships before grants, projections, trusted commands and RLS approval. | Owner/security | First client-accessible SQL migration | Not resolved | Design RLS/internal-schema boundaries | Design direction adopted; implementation pending |
| REL-001 | Domain events, idempotency and fan-out | Recommended | Write a minimal durable `domain_events` outbox row with the domain transaction; one worker alone creates channel-specific notifications with retries/dead-letter handling. | Current deterministic IDs protect multi-row workflows; dual notification writers and historical replay would duplicate outcomes. | Best-effort fan-out; command plus worker inserts; audit log as outbox; full event sourcing | Event identity/payload are immutable; delivery lifecycle is worker-owned; uniqueness is event/recipient/channel; imported history is suppressed. | Technical/security | First trusted-operation implementation | Not resolved | Design messaging conclusions, `D7` and fan-out command | Design direction adopted; implementation pending |

## 3. Documentation synchronization

- PR #46 synchronized the normalized zero-to-many wording in `06_CUTOVER_AND_ROLLBACK_PRINCIPLES.md` before this SQL slice began.
- This branch records the independently verified starting `main` SHA in the current baseline and this register.

No additional documentation debt is required to interpret this branch-local schema contract. This synchronization resolves no Open approval gate.

## 4. Approval record

When a decision is resolved, update its status, recommendation, approver, resolution date, evidence/ADR link, and every dependent section. A pull-request approval alone does not silently resolve rows marked `Open`.
