# Mujahiz IQ PostgreSQL Schema Decision Register

Status: Documentation-only design register
Baseline: `main` at `1a4e5a59a37b2f1eb05d5cf8fa555d8f7dfe84d6`
Companion design: `09_POSTGRESQL_SCHEMA_DESIGN.md`

## 1. Use of this register

- `Recommended` means the schema design adopts the direction, but it is not implemented.
- `Open` requires the named approver before the stated phase.
- `Deferred` keeps a compatibility seam but excludes implementation from the initial migration.
- `Rejected` records an alternative that must not reappear without new evidence.
- Changing a resolved recommendation requires an ADR or an explicit change to this register and its dependent design sections.
- No row authorizes SQL, RLS, hosted Supabase work, Firebase changes, data movement, or Production access.

## 2. Decisions

| ID | Topic | Status | Decision / recommendation | Rationale and evidence | Alternatives considered | Schema and delivery impact | Approver | Resolve before |
|---|---|---|---|---|---|---|---|---|
| DB-001 | Primary keys | Open | Prefer UUIDv7 if supported by the approved PostgreSQL/Supabase mechanism; otherwise use UUIDv4 without changing relationships. | Public opaque keys and migration independence are required; time locality is useful at scale. | UUIDv4; identity/bigint; legacy text keys | All tables use UUID PKs; mechanism affects first SQL migration and generated types. | Technical owner | First migration SQL PR |
| DB-002 | Status representation | Recommended | Use bounded text with checks and trusted transition commands. | Status sets will evolve across staged migration; repository currently uses string unions. | PostgreSQL enums; lookup tables | Avoids enum migration coupling; every status needs a constraint and transition test. | Technical owner | First migration SQL PR |
| DB-003 | Soft deletion | Recommended | Use explicit archive/close/revoke/tombstone fields only for domains that require history. | Universal soft delete obscures domain semantics and risks leaking archived rows. | Universal `deleted_at`; hard delete only | Per-table retention and query predicates are mandatory. | Technical + product owner | Per-domain SQL PR |
| DB-004 | Money and quantity | Recommended | Use exact numeric values; prohibit floating point; record ISO currency with each commercial revision. | Current `price` is not precise enough for future comparison/awards. | integer minor units; float; RFQ-only currency | Scale and legacy price meaning remain approval gates. | Finance/product owner | RFQ data transformation |
| DB-005 | JSONB boundary | Recommended | Permit versioned bounded snapshots/metadata only; keep queryable identities, states, relationships, lines, recipients, and contacts relational. | Firestore maps/arrays require integrity and selective access. | Document-shaped JSONB port; fully decomposed metadata | JSONB keys need schemas, size limits, and no secret/record dumps. | Technical owner | First SQL PR per domain |
| ID-001 | Authentication authority | Open | Keep Firebase Auth as initial authority through provider links; design provider-neutral user PKs. | Auth migration is not required for the Firestore data migration and increases cutover risk. | Immediate Supabase Auth cutover; permanent Firebase Auth | Requires secure token validation path and later identity migration ADR. | Owner/security | Identity migration phase |
| ID-002 | Firebase UID mapping | Recommended | Preserve Firebase UID as unique provider subject, never as relational PK. | Current documents and ownership rely on UID; provider neutrality is required. | UUID only with external mapping file; UID as PK | Identity reconciliation is a migration gate; duplicate provider subject is forbidden. | Technical/security | User transformation |
| ID-003 | Platform roles | Recommended | Store temporal role assignments; initially enforce one effective platform role per user. | Role changes need auditability and current app assumes one role. | Role on profile; unrestricted many-role set | RLS and trusted operations resolve effective role from assignments. | Owner/security | RLS design |
| ORG-001 | Organization model | Open | Introduce organizations and memberships; place RFQs under Buyer organization. | Procurement, approvals, billing, and team access require a durable party boundary. | User-owned RFQs indefinitely | Existing users need organization resolution/bootstrap during migration. | Product owner | RFQ migration |
| ORG-002 | Organization membership roles | Open | Start with minimal Buyer owner/admin/member roles and temporal membership. | Avoids prematurely encoding a complex procurement hierarchy. | Platform roles reused; full configurable RBAC | Affects RLS, invitations, decisions, and billing administration. | Product/security | Organization RLS design |
| SUP-001 | Supplier ownership versus membership | Recommended | Keep verified primary ownership temporal and separate from operational memberships. | Current `accountOwnerId` and user backlink express authority, while future multi-user access has different semantics. | Owner UID on profile; membership alone | Approval/transfer/revoke must update both constructs atomically and retain history. | Product/security | Supplier SQL/RLS design |
| SUP-002 | Supplier multi-user access | Recommended | Permit a Supplier to gain multiple role-bearing members; initially one active primary owner and one profile per owner. | Supports team scale without weakening current uniqueness guardrails. | One owner forever; immediate owner-to-many | New roles/actions remain disabled until policy and UI exist. | Product/security | Membership feature phase |
| SUP-003 | Category hierarchy | Open | Stable category codes, parent links, translations, and alias mapping. | Legacy arrays mix categories, subcategories, and free-text capabilities. | Flat labels; fixed level tables | Requires governed mapping and exception list before Supplier migration. | Product/data owner | Supplier transformation |
| SUP-004 | Address normalization | Open | Start with governed governorate reference plus typed locations and service areas. | Directory filtering needs relational areas, but a complete Iraq address standard is not yet evidenced. | Free text only; full locality hierarchy now | Unmapped address text must be preserved without inventing codes. | Product/data owner | Supplier transformation |
| SUP-005 | Contact model | Recommended | Normalize contact channels with visibility, order, verification, and optional location; protect personal contact fields. | Arrays and nested branches cannot express field-level privacy reliably. | Keep contacts as JSONB; separate table per channel type | Public projection and retention require explicit review. | Product/security | Supplier SQL/RLS design |
| SUP-006 | Product catalog depth | Deferred | Implement Catalog Lite compatibility only; defer variants, inventory, and transactional pricing. | Current collection is empty and procurement does not require full commerce modeling. | Full catalog now; omit products entirely | Schema leaves product references/files without committing to CPQ. | Product owner | Product feature phase |
| SUP-007 | Duplicate prevention | Recommended | Use versioned protected digests in a trusted-only table with atomic active uniqueness. | Current duplicate and canonical guard collections encode critical concurrency/security invariants. | Public hashes; fuzzy search only; literal three-table port | Normalizer version/key rotation and approve-transition rules need implementation ADR. | Security/technical | Supplier migration SQL PR |
| RFQ-001 | RFQ party and recipients | Recommended | RFQ belongs to Buyer organization; publication creates immutable Supplier recipient rows. | UID arrays are insufficient for ownership changes, privacy, and relationship integrity. | Buyer user + UID arrays | Recipient row becomes Supplier visibility anchor and migration reconciliation target. | Product/security | RFQ SQL/RLS design |
| RFQ-002 | Quotation revision model | Recommended | One canonical RFQ/Supplier quotation with immutable normalized revisions/items and trusted current projections. | Existing V1/V2/events already require immutable history and no-op material-change detection. | Mutable response; JSONB snapshot revisions only | Submission is a trusted atomic command; browser cannot set revision/totals/events. | Product/technical | RFQ SQL PR |
| RFQ-003 | Legacy price and currency | Open | Define unit/line/total semantics, numeric scale, tax/freight, and currency before transforming response `price`. | Current evidence is insufficient to interpret commercial amounts safely. | Preserve untyped value; infer currency from locale | Blocks authoritative quotation item transformation and comparison totals. | Finance/product owner | RFQ data transformation |
| RFQ-004 | Award and value measurement | Deferred | Preserve tables for immutable award/no-award decisions and approved value baseline; do not enable yet. | Future product value must be measurable without mutating quotation history. | Derive award from status; omit value model | Approval thresholds, partial award, disclosure, and savings method remain open. | Product/finance owner | Award feature phase |
| MSG-001 | Conversation party model | Recommended | Authorize through temporal participant rows anchored to Buyer organization and Supplier. | Participant arrays/maps and cached labels do not enforce durable party membership. | Keep two UID array; generic group chat | Current two-party UX remains possible while team membership can evolve. | Product/security | Messaging SQL/RLS design |
| MSG-002 | Message edit/delete | Open | Launch relational migration with immutable messages; evaluate trusted tombstone/moderation separately. | Current messages are immutable and private; deletion affects evidence and privacy rights. | User edit window; hard delete | Determines message history, audit, attachment, and export rules. | Product/legal/security | Messaging SQL PR |
| MSG-003 | Notification retention/rendering | Open | Store a safe bilingual delivery snapshot linked to a domain event and expire by class. | Live rendering can drift; indefinite body duplication increases exposure. | Render from live data; store template parameters only | Requires class-based retention and protected-notice policy. | Product/security | Notification SQL PR |
| SEARCH-001 | Search technology | Open | Start with PostgreSQL FTS; measure Arabic/English recall and add `pg_trgm` only if justified. | Current client search loads pages and scans keyword arrays; server-side ranked search is required. | Trigram first; external search service | Determines extensions, indexes, ranking tests, and operational complexity. | Technical/data owner | Directory query migration |
| CONTENT-001 | Bilingual content | Recommended | Use row-per-locale for governed taxonomy/pages and paired columns for compact transactional snapshots. | Content translation lifecycle differs from immutable commercial delivery snapshots. | JSONB locale maps; paired columns everywhere | Locale fallback must be explicit; no silent copying between Arabic and English. | Product/content owner | Content SQL PR |
| FILE-001 | Object storage provider | Open | Keep provider-neutral `file_objects`; evaluate Supabase Storage at upload-enablement phase. | Files are disabled and no current Storage adapter or objects exist. | Firebase Storage; other object provider | Controls signing, scanning, lifecycle, regional, and RLS integration. | Technical/security | Any upload enablement |
| FILE-002 | Upload launch state | Recommended | Keep uploads disabled until provider, validation, scanning, access, and retention decisions are implemented and tested. | Repository exposes metadata/placeholders and HTTPS links only. | Enable while migrating metadata | Prevents inventing unsafe object records or public paths. | Owner/security | Upload feature approval |
| BILL-001 | Billing subject | Open | Prefer organization-owned billing/entitlements with explicit user fallback for legacy access. | Team procurement points to organizations, while current grants are user-scoped. | User-only billing; Supplier billing first | Affects customer mapping, entitlement joins, invoicing roles, and migration. | Business/product owner | Billing design phase |
| BILL-002 | Billing event integrity | Recommended | Authenticate and deduplicate provider events into immutable ledger, then derive subscription and entitlement projections. | Billing cannot trust browser writes or webhook retries. | Mutate subscription directly from webhook; client state | Provider choice and raw-event retention remain open; no payment credentials stored. | Finance/security | Billing implementation |
| AUD-001 | Audit/security retention | Open | Approve class-based duration, minimization, legal hold, and pseudonymization rules. | Current audit volume is significant and data classes have different risks. | Universal duration; indefinite retention | Blocks final deletion jobs, partitions, and migration selection. | Owner/legal/security | Audit migration |
| RES-001 | Data residency | Open | Select region/provider after legal, Supplier-data, latency, backup, and recovery review. | Hosted Supabase environment is not yet verified or authorized. | Default region; self-hosting | Blocks hosted Production project creation and data movement. | Owner/legal/security | Hosted environment creation |
| MIG-001 | Legacy identifiers | Recommended | Preserve Firestore document IDs as unique alternate keys and maintain batch record mappings. | Rollback, reconciliation, links, and deterministic-event traceability require them. | Reuse as PK; discard after cutover | Mapping retention and exposure are restricted; PK remains UUID. | Technical owner | First data migration |
| MIG-002 | Environment strategy | Open | Use separate local, development, staging, and Production projects with explicit environment gates. | Local stack is stopped; no hosted environment has been verified; Production writes need approval. | Shared hosted project; local + Production only | Determines CI, secrets, project references, backups, and promotion controls. | Owner/technical lead | Any hosted project creation |
| SEC-001 | RLS delivery | Recommended | Design and review RLS separately after schema approval, with negative cross-party tests. | This task is schema documentation; authorization needs its own Extra High Security review. | Combine schema and RLS in one implementation PR | No client-accessible migration may ship before RLS approval. | Owner/security | First client-accessible SQL migration |
| REL-001 | Domain events/idempotency | Recommended | Use transactionally written domain events and scoped idempotency keys for multi-row commands and fan-out. | Current deterministic IDs protect publish, revision, ownership, notification, and access workflows. | Best-effort fan-out; audit log as outbox | Requires worker retry/poison policy and per-command retention. | Technical/security | First trusted-operation implementation |

## 3. Approval record

When a decision is resolved, update its status, recommendation, approver, resolution date, evidence/ADR link, and every dependent section. A pull-request approval alone does not silently resolve rows marked `Open`.
