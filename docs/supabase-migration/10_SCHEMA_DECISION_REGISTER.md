# Mujahiz IQ PostgreSQL Schema Decision Register

Status: Design register with local SQL-slice evidence
Baseline: `origin/main` at `3c66e52c011ec686c3a9b7d1dbe9db18a4800a07`, the merge of PR #118 after merged PRs #113–#117
Companion design: `09_POSTGRESQL_SCHEMA_DESIGN.md`

## 1. Use of this register

- `Recommended` means the schema design adopts the direction, but it is not implemented.
- `Open` requires the named approver before the stated phase.
- `Resolved` means the named decision is approved or settled for the explicitly recorded scope and evidence; it does not imply implementation unless the row says so.
- `Partially implemented` means the declarative schema contract exists but a named runtime or hosted gate remains.
- `Deferred` keeps a compatibility seam but excludes implementation from the initial migration.
- `Rejected` records an alternative that must not reappear without new evidence.
- Changing a resolved recommendation requires an ADR or an explicit change to this register and its dependent design sections.
- No row authorizes SQL, RLS, hosted Supabase work, Firebase changes, data movement, or Production access.
### Second SQL-slice implementation evidence

Migration 20260804000200_provider_neutral_identity_foundation.sql implements only public.user_profiles and internal.identity_provider_links on the local implementation branch. It applies the DB-001 UUIDv4 convention to both keys and the ID-002 provider-subject separation/active uniqueness boundary, with synthetic pgTAP and catalog evidence recorded in 14_SECOND_SQL_SLICE_IMPLEMENTATION_EVIDENCE.md. ID-001 remained **Open** when the local tables were implemented because they did not select an Auth authority, validate provider tokens, integrate Firebase or Supabase Auth, expose browser access, or create verified-only benefits. It is now Resolved by approved document 33 without changing that implementation boundary.

### Sixth SQL-slice selection evidence

Merged PR #66 supplies the bounded Supplier-location product/data contract in `24_SUPPLIER_LOCATION_PRODUCT_AND_DATA_CONTRACT.md`. The successor decision in `25_SIXTH_SQL_SLICE_SUPPLIER_LOCATIONS_SELECTION.md` selects only one empty, revoked, local-only `public.supplier_locations` table for a later separately authorized implementation. Contact linkage remains owned by the future `supplier_contacts.location_id`; organizations remain indirect through Supplier profiles; mapping artifacts are required before data transformation rather than empty DDL; and Auth, RLS, search, RFQ logic, audit events, hosted work, Firebase, and Production remain excluded.

At that historical selection checkpoint, the register recorded 12 unresolved gates; subsequent owner approvals supersede that count.

### Supplier-category assignment contract and seventh-slice selection evidence

The Product/Data Owner approved Option B in `27_SUPPLIER_CATEGORY_ASSIGNMENT_PRODUCT_AND_DATA_CONTRACT.md`: one reviewed temporal Supplier/category classification, primary/secondary roles, no inferred primary from legacy array order, category aliases separated from empty structural DDL, bounded provenance, terminal historical lifecycle, active duplicate/primary prevention, trusted activation checks, lossless exception handling, transformation evidence, and field-minimized future projections.

All required assignment successor-contract decisions under SUP-003 are satisfied. SUP-003 was already Resolved and remains closed; the approval was not a new decision ID and changed none of the then-current 12 unresolved gates. Merged PR #73 implements exactly the approved empty, revoked, local-only `public.supplier_category_assignments` table plus focused synthetic pgTAP as the seventh local SQL slice. Transformation remains blocked on separately approved mapping, alias-if-used, primary-selection, exception, reconciliation, rollback, environment, and data-migration evidence.

### Supplier capability approval and payment-option deferral

On 7 August 2026, the Product/Data Owner approved the capability portion of `28_SUPPLIER_CAPABILITIES_AND_PAYMENT_OPTIONS_PRODUCT_AND_DATA_CONTRACT.md`: `imports_outside_iraq` is an indicative sourcing capability; `import_only` remains unresolved and excluded; controlled/custom capability shapes require provenance, review, lifecycle, normalization, and semantic duplicate prevention; category scope is optional; capabilities are indicative; a future public projection is labels-only; restricted evidence stays private; and `official_invoice` is a documentary capability.

The same owner decision selects one future empty, revoked, local-only `public.supplier_capabilities` table as the eighth SQL slice and defers `public.supplier_payment_options`. It creates no decision ID, authorizes no SQL/data work, and resolved none of the then-current 12 gates. At that checkpoint RFQ-003 remained the legacy quotation price/currency gate; it is now Resolved by owner-approved document 38. BILL-001 remains the platform billing-subject gate; SEC-001 still owns later client-accessible RLS delivery.

### Post-PR #102 structural, identity-context, and claimant-read implementation state

PR #109 implements the nineteenth local-only Claim submit hotfix slice and PR #110 implements the twentieth local-only Claim withdraw slice after the prior structural, identity-context, Claimant self-read, and submit foundations. Current `main` contains 24 physical tables and 22 implemented concepts across 24 tracked local migrations and 24 pgTAP files; the Owner-approved catalog now contains 37 Core Phase 1 concepts, with 15 unimplemented; PR #113 evidence records 50/50 focused and 1,399/1,399 full assertions, and PR #114 evidence records 59/59 focused and 1,458/1,458 full assertions. Claim self-read RLS remains one policy, no mutation RLS policy exists, and exactly four of the six approved Claim business commands remain unimplemented: `supplier_claim.assign_reviewer`, `supplier_claim.approve`, `supplier_claim.reject`, and `supplier_claim.expire`. PR #108 identified M-1, M-2, and M-3; PR #109 corrects them locally. PR #109 corrects submit and PR #110 applies the corrected completed-binding and two-phase reservation/fence behavior to withdraw. `supplier_claim.reserve_submit` and `supplier_claim.reserve_withdraw` are private Phase-A support, not business commands. This does not imply gateway, hosted Supabase, Firebase signed-token, Production, or deployment readiness.

On 8 August 2026, the Product/Data Owner approved `29_SUPPLIER_PAYMENT_OPTIONS_PRODUCT_AND_DATA_CONTRACT.md` for payment options only: indicative Supplier-profile assertions; methods `cash|bank_transfer|cheque|letter_of_credit`; currencies `IQD|USD`; explicit `credit_not_offered` with absence-as-unknown; 1-365 calendar-day credit using only exact `invoice_date|delivery_acceptance_date` starts; separate reviewed 1-100 advance percentage; internal-only notes/provenance/review/ambiguous values; no client projection; and no invented freshness or reviewer authority. Transaction documents remain independently authoritative.

The payment-options owner decision selected exactly one empty, revoked, local-only `public.supplier_payment_options` table as the ninth SQL slice, later implemented by merged PR #78. It resolved none of the then-current 12 gates and authorized no mapping, data movement, RLS, or hosted work. At that checkpoint RFQ-003 remained the legacy quotation price/currency gate; it is now Resolved by owner-approved document 38. BILL-001 remains the platform billing-subject gate; SEC-001 still owns any later client-accessible RLS/projection delivery.

### Supplier contacts contract and merged tenth slice

The owner-approved `30_SUPPLIER_CONTACTS_PRODUCT_AND_DATA_CONTRACT.md` separates endpoint channel (`phone|email|website`), technical phone kind, subject (`company|named_person|unspecified`), company/location scope, purpose, preference rank, verification, privacy/legal basis, and disclosure. Every row belongs to one Supplier; an optional location must be a physical row for that same Supplier through a supporting location uniqueness object and composite FK. Ambiguous Firebase values, person-channel attachment, location, purpose, ownership/control, verification, and consent are never inferred.

SUP-005 is Resolved. Merged PR #80 implemented exactly the approved empty, fully revoked, local-only `public.supplier_contacts` table plus only the narrow supporting location object as the tenth local SQL slice. Rows, mapping, trusted authority, RLS/projections, and data work remain separately gated; the then-current unrelated gates were unchanged.

### REL-001 Option D approval

The owner approved `31_REL_001_IDEMPOTENCY_AND_DOMAIN_EVENTS_FOUNDATION_CONTRACT.md` and Option D on 8 August 2026. REL-001 was decision-complete and Resolved for the historical planning decision to implement neither `internal.idempotency_keys` nor `internal.domain_events` at that checkpoint. Document 39 later satisfied the producer/consumer revisit condition through `supplier_ownership.decide_claim` and one Claim-decision notification materializer.

Merged PR #95 implemented exactly the coherent empty, fully revoked, local-only `internal.idempotency_keys` and `internal.domain_events` foundation plus focused synthetic pgTAP as the fourteenth local migration. No row, trusted command, event worker/materializer, audit/notification runtime, RLS/Auth bridge, hosted work, Firebase access, Production/TEST data work, migration, or deployment was implemented.

### SUP-001 ownership and Claim approval

The Product/Data/Security Owner approved `32_SUPPLIER_OWNERSHIP_AND_CLAIM_PROFILE_PRODUCT_DATA_SECURITY_CONTRACT.md` on 8 August 2026: one temporal primary human controller per Supplier, one user across multiple Suppliers, future delegates/admins in `supplier_memberships`, optional/separate organizations, private Claim lifecycle/evidence, separate transfer/revocation, same-Supplier privacy, Firebase reconciliation, and the future `supplier_ownership.decide_claim` contract for design purposes only. Merged PR #85 implements only the selected empty, fully revoked, local-only ownership foundation.

On 9 August 2026, the same Owner approved document 41 Option A: exactly one empty, fully revoked, local-only `public.supplier_ownership_claims` table as the Claim-specific slice after REL. Claim v1 has one durable write-once assigned reviewer, no reassignment, and no Owner override. Evidence remains a bounded private aggregate on the Claim; no separate evidence, reviewer, history/event, idempotency, rate-limit, attachment, file, or notification table is needed. PR #96 records the selection only; merged PR #99 implements the empty structural table as the fifteenth tracked local migration without RLS, policies, grants, trusted commands, Auth bridge runtime, real rows, hosted authority, or Production behavior.

### Approved ID-001 contract

On 8 August 2026, the Product/Security/Data Owner approved `33_ID_001_IDENTITY_AUTHORITY_AND_PRIVILEGED_ACTOR_CONTRACT.md` as the complete hybrid Firebase-authority, provider-neutral identity/link, verification mirror, privileged-actor, lifecycle, actor-provenance, reconciliation, and Claim dependency contract. ID-001 is Resolved. `public.platform_role_assignments` is required but insufficient before relational Claim runtime and is selected as the next identity/access structural SQL candidate as one empty, fully revoked, local-only table. At that checkpoint, the 11 unrelated unresolved gates were unchanged; no SQL or runtime was authorized.

SUP-001 is Resolved. Merged PR #85 implements the approved empty, fully revoked, local-only `public.supplier_ownerships` table only; no real row, Claim table, command runtime, RLS/Auth, audit, notification, reliability runtime, Firebase, hosted, or data work is authorized.

### Owner-approved platform-role successor contract

On 8 August 2026, the Product/Security/Data Owner approved `34_PLATFORM_ROLE_ASSIGNMENTS_PRODUCT_SECURITY_DATA_CONTRACT.md`: exactly `owner|admin`; Claim reviewer remains a versioned work assignment rather than a platform role; many holders per role but only one effective active role per user; `active|revoked|expired|superseded` history; Owner-only ordinary grant authority; future protected bootstrap of at least two usable Owners plus external governance evidence; fail-closed identity/reconciliation; and the empty fully revoked `public.platform_role_assignments` table selected as the next local SQL slice after PR #85.

Merged PR #87 later implemented exactly the approved empty, fully revoked, local-only `public.platform_role_assignments` foundation plus focused synthetic pgTAP. It created no real role rows or authority and changed none of the then-current 11 unresolved gates.

### Owner-approved privileged access and security eligibility successor contract

On 10 August 2026, the Product/Security/Data Owner approved [document 55](55_PRIVILEGED_ACTOR_ACCESS_AND_SECURITY_ELIGIBILITY_READINESS.md) as the complete implementation prerequisite under existing ID-001, ID-003, AUD-001, and SEC-001. No new decision ID or named gate was created.

Access Option A is Approved: `public.access_grants`, initially and only for `platform_administration`, with each grant bound to its exact platform-role assignment and immutable `active|revoked|expired|superseded` history. Usable Owner access is non-expiring while its role is usable. Admin access is finite at no more than exactly 180 calendar days from `valid_from`, has no automatic renewal, and every new or renewed grant requires a currently usable Owner authorizer plus a distinct currently usable Owner reviewer; the subject cannot self-approve. Admin cannot administer access. Access Option B is Deferred / Not selected and Access Option C is Rejected.

Security Option A is Approved: exactly one separate, non-exposed `internal.security_eligibility_assessments` relation with scope initially `platform_administration`, result exactly `clear|deny|unknown`, initial conditions exactly `complete_clear|explicit_deny|security_hold|identity_quarantine|reconciliation_required`, lifecycle `active|resolved|expired|superseded`, immutable history, and one effective current assessment per subject/scope/current policy/coverage boundary. Only current, supported, coverage-complete `clear` plus `complete_clear` permits. Systems may restrict only and never self-clear. Human clear/release requires two distinct currently usable Owners, neither the subject. Security Option B is Deferred and Options C/D are Rejected.

Genuine authoritative security loss makes the affected Owner unusable even if they are the final usable Owner and triggers separately governed emergency recovery. Ordinary discretionary role/access closures remain prohibited from intentionally leaving zero usable Owners, and final-usable-Owner-affecting authority changes retain distinct usable-Owner review. Future bootstrap atomically composes at least two Owner assignments, matching administration access, explicit current complete-clear assessments, AUD-001 evidence, and external governance/manifest evidence. Audit classification is `privilege_security_authority`; no access/security domain event is approved without a named consumer.

PRs #113/#114 implemented the two selected empty, fully revoked, local-only structural slices, PR #116 implemented the bounded local current-principal resolver, and PR #118 implemented the bounded local target-Supplier conflict resolver: `public.access_grants` first, then `internal.security_eligibility_assessments`. The catalog therefore adds the assessment as a distinct Core Phase 1 logical concept rather than replacing deferred `security_events`: total concepts become 80 and Core Phase 1 becomes 37. Current implementation is 22 concepts, 24 physical tables, 24 migrations, and 24 pgTAP files; 15 Core Phase 1 concepts remain unimplemented. The two structural tables remain empty and inert; the resolver is read-only and local-only, while no command, bootstrap, RLS mutation, gateway, hosted authority, migration, data, or Production work is authorized by this approval.

The approved later sequence after the implemented relational privileged-actor and target-Supplier conflict resolvers is Reviewer private-read substrate, `supplier_claim.assign_reviewer`, access/security administration and bootstrap runtime, separate later approve/reject Claim slices, and separate expiry. Gateway/signed-token/pool proof, hosted work, migration/cutover, and release remain independent.

### Owner-approved AUD-001 successor contract

On 8 August 2026, the Product/Security/Data Owner approved `35_AUD_001_AUDIT_EVIDENCE_AND_TRUSTED_MUTATION_CONTRACT.md`: strict separation between durable audit evidence, domain events/idempotency, domain/application history, operational telemetry, and analytics; the minimum privileged action scope; provider-neutral actor model; append-only minimized record; retention classes; Claim/role transaction behavior; and trusted-only access. AUD-001 is Resolved, and exactly one empty, fully revoked, local-only `internal.audit_logs` table is selected as the next SQL slice independently of REL-001.

The approval authorized no SQL, audit rows, retention job, trusted command, Claim/role runtime, RLS/grants, REL/MSG implementation, hosted work, data movement, or Production action. Exact retention/legal/privacy/access/enforcement decisions remain required before real rows but did not block the empty foundation.

### Owner-approved SEARCH-001 contract

On 9 August 2026, the Product/Technical/Data Owner approved `37_SEARCH_001_POSTGRESQL_SEARCH_TECHNOLOGY_CONTRACT_REVIEW.md` Option A: relational filters plus bilingual PostgreSQL FTS first, no initial `pg_trgm`, and no external Phase 1 search service. SEARCH-001 is Resolved for architecture only. The approved contract fixes the audience-safe field boundary, original-preserving versioned normalization, same-script-first bilingual behavior, optional bounded AI intent role, deterministic ranking, query-bound keyset pagination, engineering-target treatment, four-gate trigram boundary, separate protected Admin exact lookup, payment/credit exclusion, and conditional Claim activation.

This approval selects no implementation slice and authorizes no search SQL, extension, index, projection, RPC, RLS, frontend, Gemini/AI, hosted Supabase, data migration, or runtime. At the post-PR #93 checkpoint, exactly 9 gates remained Open: `ORG-001`, `ORG-002`, `RFQ-003`, `MSG-002`, `MSG-003`, `FILE-001`, `BILL-001`, `RES-001`, and `MIG-002`.

### Owner-approved RFQ-003 Option B contract

On 9 August 2026, the Product/Finance Owner approved `38_RFQ_003_PRICE_CURRENCY_TAX_FREIGHT_CONTRACT_REVIEW.md` Option B. RFQ-003 is Resolved for explicit net unit price, derived line/quotation subtotals and total, IQD/USD single-currency revisions, exact scale-4 half-even decimals, mandatory tax/freight modes, no first-release discount or zero-price model, complete same-currency comparison, immutable canonical revisions, and future award references to trusted normalized revisions only.

The approval requires ambiguous legacy `price` to remain quarantined source evidence absent separately approved record-specific provenance. It selects no RFQ/quotation or amount-bearing SQL slice and authorizes no SQL, RLS, command, runtime, frontend, FX, award, migration/data transformation, Firebase/Production access, hosted operation, or deployment. At the post-PR #92 base, exactly 8 gates remained Open: `ORG-001`, `ORG-002`, `MSG-002`, `MSG-003`, `FILE-001`, `BILL-001`, `RES-001`, and `MIG-002`.

### Owner-approved MSG-003 Option C contract

On 9 August 2026, the Product/Security/Data/Privacy Owner approved `39_MSG_003_NOTIFICATION_RETENTION_RENDERING_AND_MATERIALIZATION_CONTRACT.md` Option C. MSG-003 is Resolved for one live authority path from trusted domain command to immutable domain event to exactly one notification materializer and one immutable safe bilingual `in_app` snapshot, with provider-neutral self-only recipients, controlled targets, nullable `read_at`, class-based lifecycle, migration fan-out suppression, and deterministic `(domain_event_id, recipient_user_profile_id, channel)` uniqueness.

This approval satisfied REL-001 Option D's producer/consumer revisit condition. Merged PR #95 subsequently implemented both `internal.idempotency_keys` and `internal.domain_events` together as an empty, fully revoked, local-only structural foundation. No REL processing, notification table/row, worker, trusted Claim/event/materializer runtime, RLS, Auth bridge, hosted Supabase, data operation, migration, or deployment is implemented. Exactly 7 gates remain Open: `ORG-001`, `ORG-002`, `MSG-002`, `FILE-001`, `BILL-001`, `RES-001`, and `MIG-002`.

### Owner-approved SEC-001 Claim-v1 architecture

On 9 August 2026, the Product/Security/Data Owner approved [document 42](42_CLAIM_FIRST_RLS_AND_TRUSTED_AUTHORIZATION_SECURITY_CONTRACT.md). SEC-001 is Resolved for Claim-v1 security architecture only: a server-mediated Firebase bridge, provider-neutral PostgreSQL principal, default-deny base relations, field-minimized projections, forced RLS, dedicated no-`BYPASSRLS` runtime identities, registered trusted commands/workers, and deterministic positive/negative/race/replay proof before client access.

PR #98 proves local Auth feasibility only; PR #99 implements the empty Claim table; PR #101 identity context; PR #102 Claimant self-read FORCE RLS and one policy; PR #103 submit; PR #108 the merged review identifying M-1/M-2/M-3; and PR #109 their local correction. Reviewer and gateway/HMAC/pool readiness remain documentation/design only; the 7 Open gates remain unchanged.

### Owner-approved Claim-v1 trusted-command architecture

On 9 August 2026, the Product/Security/Data/Operations Owner approved [document 46](46_CLAIM_V1_TRUSTED_COMMAND_ATOMICITY_AND_LOCKING_CONTRACT.md). CLAIM-CMD-001 is Resolved for implementation architecture: exactly six external commands; no external supersede or separate begin-review; exact 720-hour trusted-time validity; Owner-only write-once reviewer assignment to a distinct usable Owner/Admin; exclusive `internal.idempotency_keys` replay protection; bounded `internal.domain_events` transition facts; AUD-001-classified evidence; one deterministic Supplier lock/re-read protocol; atomic approval/ownership/competitor supersession; asynchronous notification materialization; minimal safe responses; no live historical replay; and no Claim deletion.

PR #109 implemented the corrected local two-phase `supplier_claim.submit` protocol, and PR #110 implemented local `supplier_claim.withdraw` with durable Phase-A reservation and separately fenced Phase-B execution. Exactly two of six approved Claim business mutations are implemented. `supplier_claim.reserve_submit` and `supplier_claim.reserve_withdraw` are private Phase-A support, not business commands. `supplier_claim.assign_reviewer`, `supplier_claim.approve`, `supplier_claim.reject`, and `supplier_claim.expire` remain unimplemented. The 7 Open gates remain exactly unchanged.

## 2. Decisions

| ID | Topic | Status | Decision / recommendation | Rationale and evidence | Alternatives considered | Schema and delivery impact | Required approver | Must resolve before phase | Resolution date | Evidence / reference | Resolved state |
|---|---|---|---|---|---|---|---|---|---|---|---|
| DB-001 | Primary keys | Resolved | Use database-generated UUIDv4 through `pg_catalog.gen_random_uuid()` for the local SQL slices. UUIDv7 remains preferred only if a later approved environment exposes a stable reviewed mechanism. | Local Supabase CLI `2.111.0` runs PostgreSQL `17.6`; catalog inspection found no UUIDv7 function and found built-in `pg_catalog.gen_random_uuid()` plus installed UUIDv4 mechanisms. Public opaque keys and migration independence remain required. | Custom UUIDv7 function; third-party extension; JavaScript/client-generated UUID; identity/bigint; legacy text keys | Every table in the implemented local SQL slices uses the same database UUIDv4 default. Hosted compatibility remains a later validation gate and relationships do not depend on UUID version. | Technical owner | First migration SQL PR | 2026-08-04 | SQL foundation document; migration `20260804000136`; local PostgreSQL capability query | Resolved for the local first SQL slice; hosted version/capability not claimed |
| DB-002 | Status representation | Recommended | Use bounded text with checks and trusted transition commands. | Status sets will evolve across staged migration; repository currently uses string unions. | PostgreSQL enums; lookup tables | Avoids enum migration coupling; every status needs a constraint and transition test. | Technical owner | First migration SQL PR | Not resolved | Design `B` | Design direction adopted; implementation pending |
| DB-003 | Soft deletion | Recommended | Use explicit archive/close/revoke/tombstone fields only for domains that require history. | Universal soft delete obscures domain semantics and risks leaking archived rows. | Universal `deleted_at`; hard delete only | Per-table retention and query predicates are mandatory. | Technical + product owner | Per-domain SQL PR | Not resolved | Design `F` | Design direction adopted; implementation pending |
| DB-004 | Money and quantity | Recommended | Use exact `numeric(20,4)` canonical values, prohibit floating point, record IQD/USD currency per revision, serialize four fractional digits, and round half-even at the approved arithmetic boundary. | RFQ-003 Option B now fixes new quotation monetary semantics while legacy ambiguous `price` remains quarantined source evidence. | Integer minor units; float; RFQ-only currency | New quotation commands follow owner-approved document 38; implementation remains pending and no legacy value enters the contract without approved record-specific provenance. | Finance/product owner | RFQ SQL and any RFQ data transformation | Not resolved | Design `B`, canonical no-op hash, and document 38 | Design direction synchronized with RFQ-003; implementation pending |
| DB-005 | JSONB boundary | Recommended | Permit versioned bounded snapshots/metadata only; keep queryable identities, states, relationships, lines, recipients, and contacts relational. | Firestore maps/arrays require integrity and selective access. | Document-shaped JSONB port; fully decomposed metadata | JSONB keys need schemas, size limits, and no secret/record dumps. | Technical owner | First SQL PR per domain | Not resolved | Design `B` | Design direction adopted; implementation pending |
| ID-001 | Hybrid authentication authority and privileged-actor boundary | Resolved | Adopt document 33: Firebase Auth is the sole hybrid authentication/email-verification/disablement authority; `public.user_profiles` is the stable provider-neutral domain principal; `internal.identity_provider_links` is the exact non-domain provider mapping/trusted mirror; usable Owner/Admin additionally requires temporal `public.platform_role_assignments`, separate administration access, current profile/provider/security eligibility, and Claim-specific assignment/conflict checks. | This is the smallest provider-neutral model that preserves current Firebase authority, provider portability, exact UID linkage, fail-closed verification/lifecycle behavior, accountable actors, and final-Owner safety without designing Supabase Auth. | Immediate Supabase Auth; provider UID/email/role on profile as domain authority; Firestore role inside a relational command; provider-link or JWT application role as domain privilege | The empty, fully revoked, local-only `public.platform_role_assignments` foundation is selected as the next identity/access structural SQL candidate but grants no authority. Real rows/runtime also require bootstrap/reconciliation, access, audit/security, trusted commands, RLS, and named Claim delivery gates. | Product/Security/Data Owner | Resolved for hybrid contract; implementation required before identity integration, real rows, or Claim runtime | 2026-08-08 | `33_ID_001_IDENTITY_AUTHORITY_AND_PRIVILEGED_ACTOR_CONTRACT.md`; explicit Owner approval recorded in PR #83 | Approved complete contract and empty structural candidate selection; no SQL or runtime implementation |
| ID-002 | Firebase UID mapping | Recommended | Preserve Firebase UID as unique active provider subject, never as relational PK. | Current documents and ownership rely on UID; provider neutrality is required. | UUID only with external mapping file; UID as PK | Identity reconciliation and active-row uniqueness are migration gates. | Technical/security | User transformation | Not resolved | Design `B` and `D1` | Design direction adopted; implementation pending |
| ID-003 | Platform roles and role-backed administration access | Resolved | Adopt documents 34 and 55: temporal `owner|admin` assignments remain necessary but insufficient; `public.access_grants` is bound to the exact assignment and initially only represents `platform_administration`; usable Owner access is non-expiring, Admin access is capped at exactly 180 calendar days with no auto-renewal, and each new or renewed Admin grant requires two distinct currently usable Owners with no self-approval. | Separate role and access facts preserve independent restriction, history, reconciliation, and command-specific role policy without importing trial/subscription/billing or generic RBAC/ABAC. | General multi-purpose access now; role row as access; access flag on role | PR #113 implements the empty fully revoked `public.access_grants` structural foundation, and PR #116 implements the bounded current-principal relational resolver over the local role/access/security facts, and PR #118 implements the bounded target-Supplier conflict resolver over the supported local ownership/Claim subset. Real role/access rows, role/access administration commands, dual-control runtime, final-Owner operational guard, bootstrap, Reviewer substrate, RLS delivery, gateway, hosted, and Production authority remain absent. | Product/Security/Data Owner | Resolved for contract and local structural/resolver boundary; administration/runtime work remains separate | 2026-08-10 | `34_PLATFORM_ROLE_ASSIGNMENTS_PRODUCT_SECURITY_DATA_CONTRACT.md`; `55_PRIVILEGED_ACTOR_ACCESS_AND_SECURITY_ELIGIBILITY_READINESS.md`; explicit Owner approval in Draft PR #112 | Approved Access Option A with local platform-role/access structural foundations and the bounded current-principal resolver; no real role/access rows, administration command/runtime, bootstrap, Reviewer substrate, gateway, hosted, or Production authority |
| ORG-001 | Organization model and nullable bootstrap | Open | Keep organization linkage nullable/deferred; migrate users and Suppliers without fabricated organizations; place future RFQs under organizations only after approved evidence-based bootstrap. | No current organization collection exists and free text does not prove a legal entity. | User-owned RFQs indefinitely; auto-create about 480 Supplier organizations | Initial RFQs retain Buyer creator/context; Supplier profiles remain independent; organization table is Future-Compatible and uncreated initially. | Product owner | RFQ migration | Not resolved | Design phase manifest, initial slice and identity conclusions | Unresolved approval gate |
| ORG-002 | Organization membership roles and bootstrap | Open | After ORG-001 approval, start with minimal temporal Buyer owner/admin/member roles and one active user/organization membership; never infer membership from free text. | Avoids prematurely encoding a complex procurement hierarchy or reusing platform roles. | Platform roles reused; full configurable RBAC; automatic membership import | Affects invitations, RLS, decisions and billing; current users preserve context without membership rows. | Product/security | Organization RLS design | Not resolved | Design `D1` and organization-bootstrap command | Unresolved approval gate |
| SUP-001 | Supplier ownership versus membership and Claim boundary | Resolved | Adopt document 32 Option B for ownership and document 41 Option A for Claim structure: one active temporal primary human controller per Supplier; ordinary Claims only for unowned Suppliers; exactly one private `supplier_ownership_claims` aggregate with bounded immutable evidence, controlled lifecycle, and one durable write-once reviewer with no reassignment or Owner override. | Exact two-sided Firebase backlinks are current authority evidence only while Firebase is authoritative. A listing, email/domain, organization text, membership, role, reviewer assignment, or Claim approval cannot independently prove current ownership. | Long-term dual backlinks; membership alone; several co-owners; organization-only owner; Claim table in the empty ownership slice; separate Claim evidence, reviewer, history/event, idempotency, rate-limit, attachment, file, or notification tables | The empty ownership table and PR #99 empty Claim table foundation are implemented locally; real rows/commands remain gated; mapping quarantines every mismatch; transfer/revoke preserve temporal history. | Product/Data/Security Owner | Resolved for ownership and Claim structural selections; later command, ID/Auth, RLS/security, retention, delivery, hosted, and data approvals before activation | 2026-08-09 | `32_SUPPLIER_OWNERSHIP_AND_CLAIM_PROFILE_PRODUCT_DATA_SECURITY_CONTRACT.md`; `41_CLAIM_SUPPLIER_PROFILE_STRUCTURAL_AND_COMMAND_READINESS_REVIEW.md`; PR #99 structural evidence; explicit Owner approvals | Approved ownership and empty Claim foundations implemented locally; no Claim row, trusted command, RLS/Auth runtime, hosted authority, or Production implementation |
| SUP-002 | Supplier multi-user access | Recommended | Permit future role-bearing members with one active membership per Supplier/user pair regardless of role; one user may hold memberships across multiple Suppliers; retain one active primary controller per Supplier under SUP-001. | Supports team scale and multi-Supplier operators without weakening the distinction between verified primary control and delegated access. | One operator forever; several primary co-owners; organization membership alone; immediate configurable RBAC | Membership table is Future-Compatible and uncreated initially; invitations, role vocabulary, authority, and new actions remain disabled pending a separate contract. | Product/security | Membership feature phase | Not resolved | Phase manifest, `D2`, and document 32 | Design direction synchronized with SUP-001 proposal; implementation pending |
| SUP-003 | Category hierarchy | Resolved | Use the approved Supplier-offering taxonomy contract: stable immutable codes, one-parent hierarchy with maximum depth three, paired Arabic/English labels, category-specific aliases, explicit lifecycle and mapping outcomes, leaf-only Supplier assignment with at most one primary, and configurable assignment limits; merge `category_translations` initially. | Legacy arrays mix categories, subcategories, and free-text capabilities. The founder-led dual-role governance decision is introduced by PR #57 and is effective only until a future governance document delegates or separates Product Owner and Data Owner. | Flat labels; fixed level tables; separate translations now | Requires governed mapping and exception list before Supplier transformation; approval does not authorize taxonomy rows, aliases, assignments, migration, or Production work beyond the separately merged empty foundation. | Product/Data owner under founder-led dual-role governance introduced by PR #57 | Supplier transformation | 2026-08-05 | `18_SUP_003_CATEGORY_TAXONOMY_CONTRACT.md` sections 1, 13-14; PR #57 and PR #59 | Approved contract; merged PR #59 implements empty `public.categories` only, while aliases, assignments, mappings, taxonomy rows, hosted work, and Production work remain excluded |
| SUP-004 | Administrative areas and Supplier assignment | Resolved | Use the approved hierarchy-ready model with a governorate-only first scope; a later authorized population must include all 19 governorates, including Halabja; district/subdistrict support is deferred; physical locations and service coverage remain separate typed assignments; `all_iraq` is a non-area coverage exception; `imports_outside_iraq` is a Supplier capability; stable internal identities remain separate from official/reference codes; ambiguous Firebase values are preserved for review. | Current `governorates[]` combines head-office selection and branch-derived values; `local_only`/`governorate_level` name no target; the repository's 18-value list omits Halabja; lower official units and codes change. | Flat governorates only; separate physical/coverage tables; full hierarchy now; free text/JSON only; infer coverage from first governorate | Preserve source coordinates, outcome/version, order, exceptions, and history; physical rows never imply coverage; ambiguous values are never invented or silently mapped. Approval selects an empty `administrative_areas` fifth slice only and does not authorize SQL or data work. | Product/data owner under founder-led dual-role governance introduced by PR #57 | Administrative-area SQL selection and Supplier transformation | 2026-08-05 | `21_SUP_004_ADMINISTRATIVE_AREAS_AND_SUPPLIER_ASSIGNMENT_CONTRACT.md` sections 1 and 3-5; explicit owner decision in PR #61 | Approved contract; proposed fifth slice is `public.administrative_areas` only, while implementation, Supplier assignments, population, migration, hosted work, and Production work remain excluded |
| SUP-005 | Contact model and audience projection | Resolved | Use one endpoint-oriented Supplier contact table: channel, phone kind, subject (`company|named_person|unspecified`), scope, purpose, preference, verification, privacy/legal basis, and disclosure remain separate; optional location is a same-Supplier physical row; all base values remain restricted and audiences use only later approved field-minimized security-invoker views/RPCs. | Arrays/nested branches cannot enforce endpoint uniqueness, person attachment, same-Supplier location integrity, or field-level privacy; RLS is not column security; ambiguous legacy evidence cannot prove ownership, verification, purpose, or consent. | Public base table; JSON/arrays/profile columns; table per channel; separate people/channels now; person-only rows | One empty fully revoked table plus only supporting location uniqueness was selected and implemented by merged PR #80 as the tenth slice; named-person/default-uncertain data remains restricted and no anonymous/public projection is approved. | Product/data/security/privacy | Resolved for empty contact SQL selection; later SEC-001 RLS design before access | 2026-08-08 | `30_SUPPLIER_CONTACTS_PRODUCT_AND_DATA_CONTRACT.md` sections 1 and 17; explicit owner decision in PR #79 | Approved empty structural boundary implemented by merged PR #80; rows, mapping, trusted commands, RLS/projections, and data work remain separately gated |
| SUP-006 | Product catalog depth | Deferred | Keep Catalog Lite compatibility as Core Later; defer variants, inventory, and transactional pricing. | Current collection is empty and procurement does not require full commerce modeling. | Full catalog now; omit products entirely | Product table is not in Core Phase 1; files remain gated. | Product owner | Product feature phase | Not resolved | Phase manifest and `D2` | Deferred by design |
| SUP-007 | Exact and fuzzy duplicate prevention | Recommended | Use versioned protected exact digests with active uniqueness and atomic pending promotion, plus a versioned bounded fuzzy-candidate projection and human review queue. | Current guard collections encode critical concurrency invariants while name similarity requires supporting evidence rather than exact equality. | Public hashes; fuzzy-only; exact-only; automatic merge; literal three-table port | Key rotation/normalizer/scorer versions are recorded; Dice or approved equivalent uses name, area/category and supporting evidence; no automatic merge/delete. | Security/technical | Supplier migration SQL PR | Not resolved | Design duplicate decision, `D2` and duplicate command | Design direction adopted; implementation pending |
| RFQ-001 | RFQ party and recipients | Recommended | Preserve transitional Buyer creator/context while organization linkage is Open; publication creates immutable Supplier recipient rows and each recipient is the sole quotation authorization anchor. | UID arrays and duplicated RFQ/Supplier quotation fields cannot enforce party integrity. | Require fabricated Buyer organization; Buyer user plus UID arrays; duplicated quotation parties | Recipient uniqueness and eligibility are publication gates and migration reconciliation targets. | Product/security | RFQ SQL/RLS design | Not resolved | Design RFQ conclusions, `D3` and commands | Design direction adopted; implementation pending |
| RFQ-002 | Quotation revision and no-op model | Recommended | Use one quotation per recipient with immutable revisions/items, temporarily nullable current pointer set in the same transaction, composite same-parent integrity, and no `quotation_items` table. | Existing V1/V2/events require immutable history; prior model had circular insertion and competing projections. | Mutable response; JSONB revisions only; required circular pointer; current-item table | Revision number is unique per quotation; canonical hash makes identical submissions true no-ops with no audit/event/notice. | Product/technical | RFQ SQL PR | Not resolved | Design RFQ conclusions, no-op hash, `D3` and command | Design direction adopted; implementation pending |
| RFQ-003 | Legacy price, currency, tax, and freight | Resolved | Adopt document 38 Option B: no automatic legacy `price` transformation; explicit positive net `unit_price` and trusted derived totals for new quotations; published quantity/unit equality; IQD/USD single-currency revisions; exact `numeric(20,4)` and half-even canonicalization; approved tax/freight modes; no structured discount, zero-price line, FX, or cross-currency ranking; complete same-currency informational comparison; immutable canonical revisions; normalized-revision-only future award/value seam. | Current UI intent cannot prove record-level legacy price basis, currency, tax, freight, quantity/unit, or revision integrity. Quarantine prevents invented money while the explicit new contract supports deterministic comparison. | Option A minimal response total; Option C permanent unknown-semantics compatibility envelope; infer legacy meaning | Ambiguous legacy units create no authoritative amount-bearing target graph absent separately approved record-specific provenance. No RFQ/quotation or amount-bearing SQL slice is selected; SQL, commands, RLS, frontend, migration/data transformation, awards, FX, hosted work, and Production access remain separately gated. | Product/Finance Owner | Resolved for architecture/product/finance/data semantics; separate authorization before RFQ SQL, transformation, or runtime | 2026-08-09 | `38_RFQ_003_PRICE_CURRENCY_TAX_FREIGHT_CONTRACT_REVIEW.md`; explicit Owner approval recorded in PR #92 | Approved Option B contract only; no SQL, runtime, migration, data, hosted, or Production implementation |
| RFQ-004 | Award and value measurement | Deferred | Preserve Future-Compatible immutable award/no-award decision and approved value-baseline concepts; do not create or enable yet. | Future product value must be measurable without mutating quotation history. | Derive award from status; omit value model | Approval thresholds, partial award, disclosure, and savings method remain open. | Product/finance owner | Award feature phase | Not resolved | Phase manifest and `D3` | Deferred by design |
| MSG-001 | Conversation party and temporal access | Recommended | Use temporal participants with inclusive `valid_from` and exclusive `valid_until`; reads are limited to messages inside that interval. | Current participant arrays cannot stop access to later messages or protect prior private history on ownership change. | Current-membership-only check; inherited full history; generic group chat | Revocation forbids later messages; normal historical access is interval-bounded; new owners receive no old messages; RFQ close/cancel closes write epochs. | Product/security | Messaging SQL/RLS design | Not resolved | Design messaging conclusions, `E` and conversation command | Design direction adopted; implementation pending |
| MSG-002 | Message edit/delete | Open | Launch the later relational messaging phase with immutable messages; evaluate trusted tombstone/moderation separately. | Current messages are immutable and private; deletion affects evidence and privacy rights. | User edit window; hard delete | Determines history, audit, attachment, export and exceptional quarantine rules. | Product/legal/security | Messaging SQL PR | Not resolved | Design messaging conclusions and `F` | Unresolved approval gate |
| MSG-003 | Notification retention/rendering | Resolved | Adopt document 39 Option C: trusted domain command to immutable domain event to exactly one notification materializer; immutable safe bilingual `in_app` snapshot with controlled target/type/template/version metadata; provider-neutral self-only recipient; nullable `read_at`; class-based lifecycle; migration fan-out suppression; and unique `(domain_event_id, recipient_user_profile_id, channel)`. | The contract preserves current bilingual inbox value while eliminating command-side dual writes, mutable live rendering, recipient drift, arbitrary links, sensitive payloads, and replay duplicates. | Direct command inserts; live rendering; template parameters only; command plus worker insertion; Firebase fallback | MSG-003 is decision-complete and satisfied REL-001's revisit condition. PR #95 implemented the coherent empty REL structure separately; no notification table or notification/event-processing runtime is selected here. | Product/Security/Data/Privacy Owner | Resolved for notification contract; separate approval before notification SQL, runtime, hosted work, or data movement | 2026-08-09 | `39_MSG_003_NOTIFICATION_RETENTION_RENDERING_AND_MATERIALIZATION_CONTRACT.md`; explicit Owner approval recorded in PR #94; PR #95 REL implementation evidence | Approved Option C contract; REL structure implemented separately; no notification row, worker, RLS/Auth, hosted, data, migration, or runtime implementation |
| SEARCH-001 | Search technology | Resolved | Adopt document 37 Option A: relational filters plus bilingual PostgreSQL FTS first; exclude initial `pg_trgm`; reject an external Phase 1 service. | Current client search loads incomplete pages and mixes authoritative, compatibility, and protected fields; the approved contract makes audience eligibility, filters, result membership, deterministic rank, and keyset pagination server-authoritative while preserving bounded AI intent only. | Trigram from day one; external Phase 1 service | Future implementation must use audience-safe projections, original-preserving versioned normalization, same-script-first ranking, reviewed aliases, exact/phrase/FTS tiers, a separate protected Admin exact lookup, and all four evidence gates before any trigram proposal. Claim is targeted first but remains inactive until separate delivery gates pass. | Product/technical/data owner | Resolved for architecture; implementation remains a separate gated phase | 2026-08-09 | `37_SEARCH_001_POSTGRESQL_SEARCH_TECHNOLOGY_CONTRACT_REVIEW.md`; explicit owner approval recorded in PR #93 | Approved Option A architecture only; no SQL, extension, index, projection, RPC, RLS, application, AI, hosted, data, or runtime implementation |
| CONTENT-001 | Bilingual content | Recommended | Use paired Arabic/English columns for bounded categories and transactional snapshots; row-per-locale remains for future governed pages where lifecycle differs. | Launch taxonomy has two required languages and does not justify `category_translations`; page translation lifecycle may differ. | JSONB locale maps; separate translation rows everywhere; paired columns everywhere | Locale fallback is explicit and no language is silently copied. | Product/content owner | Content SQL PR | Not resolved | Phase manifest and `D5` | Design direction adopted; implementation pending |
| FILE-001 | Object storage provider and custody | Open | Before approval, allow only strictly validated allowlisted HTTPS or preserved legacy references with source/provider metadata in typed attachment rows or bounded Claim evidence; include no `file_objects` column/FK. Keep future provider-neutral `file_objects`, creator/uploader provenance only, and parent-derived custody authorization. | Files/Storage are disabled; legacy references may lack size/MIME/hash; uploader identity, opaque keys, and permanent public URLs do not prove continuing domain access. | Firebase Storage; Supabase Storage; creator-owned files; public URLs; arbitrary external schemes | Controls later signing, scanning, lifecycle, region, orphan compensation and typed-parent authorization. An audited later migration may replace references with provider-neutral IDs. | Technical/security | Any upload enablement | Not resolved | Design file decision, pre-FILE-001 strategy, `D2`/`D3`/`D4`/`D6` and finalization command | Unresolved approval gate |
| FILE-002 | Upload launch state | Recommended | Keep uploads disabled until provider, validation, scanning, typed custody, access and retention decisions are implemented and tested; Phase-1 reference rows imply no Storage and private files are never permanent public URLs. | Repository exposes metadata/placeholders and controlled HTTPS links only. | Enable while migrating metadata | Prevents fake file rows/FKs, unsafe schemes/objects, public paths, or invented metadata; file tables remain uncreated initially. | Owner/security | Upload feature approval | Not resolved | Phase manifest, pre-FILE-001 strategy and `D6` | Design direction adopted; implementation pending |
| BILL-001 | Billing subject | Open | Prefer organization-owned billing/entitlements with explicit user fallback for legacy access. | Team procurement points toward organizations, while current grants are user-scoped and ORG decisions remain Open. | User-only billing; Supplier billing first | Affects customer mapping, entitlement joins, invoicing roles and migration; billing tables are uncreated initially. | Business/product owner | Billing design phase | Not resolved | Phase manifest and `D8` | Unresolved approval gate |
| BILL-002 | Billing event integrity | Recommended | Authenticate/deduplicate provider events into an immutable ledger, then derive subscription and entitlement projections. | Billing cannot trust browser writes or webhook retries. | Mutate subscription directly from webhook; client state | Provider choice/raw-event retention remain open; no payment credentials stored. | Finance/security | Billing implementation | Not resolved | Design `D8` | Design direction adopted; implementation pending |
| AUD-001 | Audit evidence and trusted mutation boundary | Resolved | Adopt document 35: accountable privileged-action scope; provider-neutral human/service/migration/worker actors; append-only minimized `internal.audit_logs`; same-transaction success evidence; minimized denial evidence; class-based retention; REL separation; and trusted-command-only writes. | Trusted relational mutations require durable investigation evidence without turning audit into an outbox, request dump, activity feed, operational log, or analytics stream. | Audit as event/outbox; general activity logging; mutable rows; full before/after payloads; provider UID actor FK; defer all audit design until runtime | One empty fully revoked local table is selected independently of REL-001. Exact registry/DDL/enforcement, retention periods/legal hold/privacy, read projections, trusted writers, and hosted/runtime proof still gate real rows. | Product/Security/Data Owner; legal/privacy/security/operations for exact retention/runtime values | Resolved for contract and empty SQL selection; remaining operational decisions before real rows/runtime | 2026-08-08 | `35_AUD_001_AUDIT_EVIDENCE_AND_TRUSTED_MUTATION_CONTRACT.md`; explicit Owner approval recorded in PR #89; design `F`; documents 31, 32, and 34 | Approved complete contract and empty structural SQL selection; no SQL or runtime implementation |
| RES-001 | Data residency | Open | Select region/provider after legal, Supplier-data, latency, backup and recovery review. | Hosted Supabase environment is not verified or authorized. | Default region; self-hosting | Blocks hosted Production project creation and data movement. | Owner/legal/security | Hosted environment creation | Not resolved | Current baseline and design scope | Unresolved approval gate |
| MIG-001 | Legacy identifiers and zero-to-many trace | Partially implemented | Preserve root Firestore IDs as source identities and use physically distinct disposition, target-binding, and merge-member relations. Every ordinary mapping has exactly one semantic child key or deterministic ordinal; active logical-slot uniqueness excludes target UUID; active reverse-target uniqueness spans ordinary and reviewed merge targets; merge contributors use a separate member relation. | Rollback, replay, discard/quarantine, normalized child expansion, non-forking mappings, unambiguous reverse trace, and content reconciliation require separate source outcome/target facts and retained superseded history. | Reuse IDs as PK; one mapping only; include target UUID in slot uniqueness; separate target tables with an unenforced cross-table reverse constraint; relax reverse uniqueness for merges; fake no-target UUID; discard superseded mappings | The local `internal` schema implements batch/version-coupled dispositions, shared target bindings/merge-group headers, target-qualified merge members, and database-generated successor lineage IDs. Declarative constraints cover active parent coupling, child slots, reverse targets, replay identity, bounded versioned metadata, passed-evidence consistency, contributor isolation, and reasoned same-lineage supersession. Runtime locking, identical-replay lookup, minimum/complete merge-member validation, atomic graph supersession, reconciliation orchestration, owner-level immutability, and rollback execution remain Migration Engine/access-control work. | Technical owner | Migration Engine runtime / first data migration | 2026-08-04 (schema contract) | SQL foundation document; migration `20260804000136`; 60/60 local database assertions | Declarative schema contract implemented locally; runtime behavior remains open |
| MIG-002 | Environment strategy | Open | Use separate local, development, staging and Production projects with explicit environment gates. | Local stack is disposable; no hosted environment has been verified; Production writes need approval. | Shared hosted project; local plus Production only | Determines CI, secrets, project references, backups and promotion controls. | Owner/technical lead | Any hosted project creation | Not resolved | Current baseline, design scope, and SQL foundation document | Unresolved approval gate; this local slice provides no hosted-environment evidence |
| SEC-001 | Claim-v1 RLS and trusted authorization delivery | Resolved | Adopt document 42 plus document 55 Security Option A: server-mediated Firebase bridge, provider-neutral current principal, default-deny protected bases, and separate restricted `internal.security_eligibility_assessments` with result `clear|deny|unknown`, initial five conditions, immutable `active|resolved|expired|superseded` history, and only current coverage-complete `clear` plus `complete_clear` permitting. Systems restrict only; human clear/release requires two distinct currently usable Owners neither the subject; genuine security loss overrides final-Owner availability and enters governed recovery. | RLS alone cannot validate Firebase authority or own multi-row transitions; absence of a blocker is not clear. A distinct current assessment relation cannot be replaced by access, profile reference, audit, or deferred event history. | Direct browser authority; `service_role`; access lifecycle as security state; separate blocker tables now; `security_events` or profile reference as current authority | PR #114 implements the one empty, non-exposed, fully revoked `internal.security_eligibility_assessments` foundation, PR #116 implements the bounded local current-principal resolver, and PR #118 implements the bounded local target-Supplier conflict resolver. Real security assessment rows, security administration/clear/restrict/release commands, Reviewer private reads, `assign_reviewer`, Firebase gateway/signed-token proof, hosted least-privilege ownership, and Production authority remain absent. | Product/Security/Data Owner | Resolved for architecture and local structural/resolver boundary; administration/runtime work remains separate | 2026-08-10 | `42_CLAIM_FIRST_RLS_AND_TRUSTED_AUTHORIZATION_SECURITY_CONTRACT.md`; `55_PRIVILEGED_ACTOR_ACCESS_AND_SECURITY_ELIGIBILITY_READINESS.md`; explicit Owner approval in Draft PR #112; PR #98/#101/#102 bounded evidence | Approved Security Option A with the local security-eligibility structural foundation and bounded current-principal resolver; no real assessment rows, security administration commands, target-Supplier conflict resolver, Reviewer private reads, `assign_reviewer`, gateway, hosted least-privilege ownership, or Production authority |
| CLAIM-CMD-001 | Claim-v1 trusted-command atomicity, locking, and side effects | Resolved | Adopt Owner-approved document 46: exactly six external commands; internal approval supersession; exact 720-hour trusted-time validity; Owner-only write-once reviewer assignment; shared REL, event, lock, time, audit, safe-result, migration, retention, and asynchronous-notification boundaries; submit first after claimant RLS/read substrate. | Single-purpose commands, complete post-lock revalidation, one Supplier serialization protocol, and atomic approval effects prevent confused authorization, stale routing authority, partial ownership, competitor leakage, duplicate side effects, and direct notification dual writes. | External supersede; separate begin-review; combined `supplier_ownership.decide_claim` runtime; all commands in one slice; local-calendar expiry; Claim-local idempotency; direct notification inserts; reassignment/override/multiple/delegated reviewers | No SQL/RLS/runtime is selected. Each command slice introduces only its bounded allowlists and tests; approval commits ownership, winner, every competitor, required audit/events, and idempotency together. Claim rows are never deleted by v1. | Product/Security/Data/Operations Owner | Resolved for architecture; separate authorization and prerequisite RLS/read substrate before implementation | 2026-08-09 | `46_CLAIM_V1_TRUSTED_COMMAND_ATOMICITY_AND_LOCKING_CONTRACT.md`; explicit Owner approval recorded in Draft PR #100 | Approved implementation architecture only; `supplier_claim.submit` and `supplier_claim.withdraw` are implemented locally; `assign_reviewer`, `approve`, `reject`, and `expire` remain unimplemented; PR #102 provides only one Claimant self-read policy and projection |
| REL-001 | Domain events, idempotency and fan-out | Resolved | Preserve the historical Option D checkpoint, then apply its approved revisit condition: document 39 establishes the first trusted producer/consumer path, and PR #95 implements `internal.idempotency_keys` plus `internal.domain_events` together as one empty, fully revoked, local-only foundation. | Empty reliability tables were rejected until a concrete producer/consumer path existed. MSG-003 supplied that path and fixed the event/materializer/dedupe boundary; PR #95 then implemented only the shared structural roots. | Idempotency only; domain events only; universally inseparable tables; best-effort fan-out; audit log as outbox; full event sourcing | Exactly both internal tables are implemented locally and remain separate from audit and notifications. No row, command integration, lease/retry/dead-letter processor, materializer, hosted access, migration, or runtime exists. | Product/technical/security Owner | Structural foundation implemented; exact operational, retention, trusted-writer, runtime, security, and hosted approvals remain | 2026-08-09 | `31_REL_001_IDEMPOTENCY_AND_DOMAIN_EVENTS_FOUNDATION_CONTRACT.md`; document 39; `40_REL_001_TWO_TABLE_FOUNDATION_IMPLEMENTATION_EVIDENCE.md`; merged PR #95 | Resolved contract and empty two-table structural foundation implemented; runtime and real rows remain unimplemented |

### PR #118 target-Supplier conflict resolver synchronization

PR #118 is merged at 3c66e52c011ec686c3a9b7d1dbe9db18a4800a07; reviewed implementation head 788574facd9ebb848a3e11098e29d9063f6d2efe is an ancestor. The private `claim_security.target_supplier_conflict_v1(uuid, uuid, uuid)` helper returns exactly `clear|conflict|unknown`; only `clear` is authorization-positive. It is bounded to supported local ownership/Claim evidence, fails closed on missing/ambiguous/contradictory/unsupported/unreadable coverage, and does not infer relationships from contacts, email/domain, organization text, account context, provenance, provider data, names, or role text. Known future membership/organization relations force `unknown`; local `clear` is not hosted no-conflict proof and conflict clear alone is never Reviewer authorization. The helper is `SECURITY DEFINER`, locally owned by `postgres`, fixed `search_path=pg_catalog`, read-only, one-clock, no dynamic SQL, and no execution is granted to `PUBLIC`, `anon`, `authenticated`, `service_role`, or `mujahiz_claim_runtime`.

The next dependency-safe task is Reviewer Private-Read Substrate — Readiness and Implementation: Owner assignment queue, reviewer candidate eligibility/projection, assigned-reviewer queue/detail, and exact RLS/object-grant boundaries. It must explicitly investigate whether a private arbitrary-profile privileged-eligibility helper is required for candidate UUID evaluation. Do not expose such a helper in this sync; `supplier_claim.assign_reviewer` remains after the read substrate.

### PR #116 bounded resolver synchronization

PR #116 is merged in origin/main before PR #118; reviewed head e1c13098e5ef93a6bd02e8040d3f325f2e0ed4b8 is an ancestor. The exact server-owned Claim-v1 tuple is sec-001-claim-v1 mapped to firebase-provider-state-v1, platform-role-policy-v1, platform-access-policy-v1, platform-admin-security-v1, platform-admin-coverage-v1, and platform-admin-minimization-v1. Equality is exact; provider-state NULL is unsupported; role and access versions are distinct.

The resolver is claim_security.current_privileged_actor_v1() with no arguments and exactly decision (eligible|denied|unknown) plus role_code. It reads only the current transaction-local Claim principal, captures one trusted timestamp, uses half-open temporal evaluation, is VOLATILE and SECURITY DEFINER with fixed search_path=pg_catalog, local owner postgres, and execute only for mujahiz_claim_runtime. It is read-only and does not create authentication, hosted authorization, a token, lease, reservation, or cache. Focused pgTAP passed 112/112, the reused-backend context/no-cache harness passed, and full local SQL validation passed 1,570/1,570. Reviewer private-read substrate and `supplier_claim.assign_reviewer` remain separate future work.

## 3. Documentation synchronization

- PR #46 synchronized the normalized zero-to-many wording in `06_CUTOVER_AND_ROLLBACK_PRINCIPLES.md` before this SQL slice began.
- Current verified `origin/main` is `3c66e52c011ec686c3a9b7d1dbe9db18a4800a07`, the merge of PR #116 after merged PRs #113, #114, and #115. Physical implementation is 24 migrations, 24 pgTAP files, 24 tables, and 22 implemented concepts. The logical catalog is 80 total / 37 Core Phase 1, leaving 15 unimplemented Core concepts. PR #113 evidence is 50/50 focused and 1,399/1,399 full assertions; PR #114 evidence is 59/59 focused and 1,458/1,458 full assertions; PR #116 evidence is 112/112 focused, a passed reused-backend context/no-cache harness, and 1,570/1,570 full assertions; PR #118 evidence is 69/69 focused and 1,639/1,639 full assertions in the final full validator. The privileged foundations remain empty/inert except for the read-only local resolver; exactly one Claim policy and zero Claim mutation policies remain.

Document 32 records the approved Claim boundary; document 47 records Claimant self-read evidence; document 48 records PR #103 submit; document 49 records PR #108's merged red-team review; document 50 records PR #107's merged withdraw implementation-readiness recommendation; document 53 records PR #109's local correction; document 54 records PR #110's local withdraw implementation evidence; document 55 records the approved access/security contract; documents 56/57 record the exact #113/#114 structural evidence. The 7 Open gates remain exactly `ORG-001`, `ORG-002`, `MSG-002`, `FILE-001`, `BILL-001`, `RES-001`, and `MIG-002`.

## 4. Approval record

When a decision is resolved, update its status, recommendation, approver, resolution date, evidence/ADR link, and every dependent section. A pull-request approval alone does not silently resolve rows marked `Open`.
